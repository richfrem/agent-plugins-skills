---
name: {name}
description: >
  {description}
argument-hint: "[optional arguments]"
---

# {name} Command

You are executing a specialized slash command (`/{name}`).
Your primary directive is: {description}

## Instructions
1. Parse the `$ARGUMENTS` passed to this command.
2. If arguments are missing or malformed, ask the user for clarification before proceeding to avoid hallucinated execution.
3. Execute the required functionality using system tools.
4. Respond concisely with the results.
