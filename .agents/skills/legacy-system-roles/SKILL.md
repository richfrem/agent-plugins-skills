---
name: legacy-system-roles
description: Standardized skill interface for legacy-system-roles.
---

# Legacy System Roles

## Overview

This skill provides access to the functionality of the **legacy-system-roles** plugin.

## Usage

Start by exploring the `scripts/` or `workflows/` directory if available.

### Available Scripts
(Run `ls -R plugins/legacy-system-roles/scripts` to see available tools)

