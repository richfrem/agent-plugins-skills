# Agentic OS v36 Review Bundle
**Description:** Flywheel architecture completion. Three major additions: (1) full loop lifecycle — every loop skill now ends with post-run survey, metrics, memory persistence (not just kernel signal exchange); (2) test registry protocol — every test cycle is documented before running with hypothesis/acceptance/failure criteria, results recorded after, prior results inform next design; (3) improvement ledger + progress reporting — longitudinal eval score progression, survey-to-action traceability, north star completion rate, autoresearch-style progress chart generated by loop-progress-report skill. Also: 50 pre-designed test scenarios seeded in references, state_increment PROTECTED_STATE_KEYS regression fixed (round 8 red-team bug).

## Index
1. `plugins/agent-agentic-os/skills/concurrent-agent-loop/SKILL.md` - v0.5.0: 15 mandatory loop steps, improvement ledger write at Stage 4.7, test registry at Stage 4.6
2. `plugins/agent-agentic-os/references/improvement-ledger-spec.md` - NEW: 3-section longitudinal tracking spec
3. `plugins/agent-agentic-os/references/test-registry-protocol.md` - NEW: test scenario documentation protocol
4. `plugins/agent-agentic-os/references/test-scenarios-seed.md` - NEW: 50 pre-designed test hypotheses (abridged — index + T01-T12)
5. `plugins/agent-agentic-os/skills/loop-progress-report/SKILL.md` - NEW skill: autoresearch-style progress chart
6. `plugins/agent-agentic-os/skills/loop-progress-report/scripts/generate_report.py` - NEW: reads results.tsv per skill, plots KEEP/DISCARD timeline
7. `plugins/agent-agentic-os/references/diagrams/loop-progress-report.mmd` - NEW: sequence diagram for data flow
8. `plugins/agent-agentic-os/agents/os-learning-loop.md` - UPDATED: test registry Phase 1, pre-eval doc Phase 3, registry close Phase 5
9. `plugins/agent-agentic-os/skills/session-memory-manager/SKILL.md` - UPDATED: Phase 3 test registry preservation, Phase 5 self-assessment survey
10. `plugins/agent-agentic-os/skills/agentic-os-init/runtime/kernel.py` - BUG FIX: state_increment now checks PROTECTED_STATE_KEYS

---

## File: `plugins/agent-agentic-os/skills/concurrent-agent-loop/SKILL.md`
> Note: v0.5.0 — now defines 15 mandatory loop steps. Core design gap from v35 review: prior version described only the kernel transport layer (signal exchange), not what a complete improvement cycle must contain. Every loop cycle is now a full flywheel turn: orientation reads ledger + test registry, execution + friction events, eval + KEEP/DISCARD, peer survey, orchestrator survey, metrics, loop report, test registry close, ledger update, memory promotion, os-learning-loop trigger check.

```markdown
---
name: concurrent-agent-loop
version: 0.5.0
description: >
  Pattern 5: Concurrent Event-Driven Multi-Agent Loop. Coordinates multiple Claude sessions
  as OS threads sharing a common event bus and memory address space. Every loop cycle is a
  full improvement cycle: execute, eval against benchmark (KEEP/DISCARD), emit friction events
  during work, close with post_run_metrics, agent self-assessment survey saved to retrospectives,
  memory persistence, and os-learning-loop trigger if friction threshold crossed.
  Four coordination topologies: turn-signal, fan-out, request-reply, dual-loop (Pattern D).
status: active
trigger: concurrent agents, shared event bus, multi-agent coordination, turn signal, fan-out,
  parallel agents shared state, event-driven agents, agent threads, kernel event bus,
  cross-session coordination, replace AGENT_COMMS, concurrent skill audit, claim task,
  inner agent, orchestrator peer agent, worker agent, continuous improvement loop,
  eval benchmark, self-assessment survey, post-run survey, friction events, metrics,
  os-learning-loop, skill improvement, memory persistence, retrospective
allowed-tools: Read, Write, Edit, Bash, Glob, Grep
---

# Concurrent Agent Loop

> Pattern 5 in the agent-loops taxonomy. Treats concurrent Claude sessions as OS threads
> sharing a filesystem address space. The kernel event bus coordinates signals. Every cycle
> includes real work, eval against benchmark, friction tracking, agent self-assessment survey,
> post-run metrics, and memory persistence. The OS learns from every run.

## CRITICAL: What a Loop Is

The kernel (kernel.py) handles event routing, locks, and cursors. A loop is NOT a signal
exchange. A complete loop cycle requires all of the following, in order:

1. Orientation - ORCHESTRATOR reads context/memory/improvement-ledger.md (score trajectory, survey-to-action trace, north star trend), test registry, last session log, last surveys
2. Test scenario documented - ORCHESTRATOR writes scenario record to context/memory/tests/ BEFORE emitting task.assigned
3. Execution - INNER_AGENT reads strategy packet, does real work
4. Friction tracking - agents emit type:friction events whenever they hit uncertainty, ambiguity, wrong CLI, or need human rescue
5. Eval against benchmark - eval_runner.py run, score compared to results.tsv baseline
6. KEEP/DISCARD verdict - PEER_AGENT runs skill-improvement-eval independently
7. PEER_AGENT self-assessment survey - answered in full, saved to context/memory/retrospectives/
8. Apply improvement or correction packet - ORCHESTRATOR acts on verdict
9. ORCHESTRATOR self-assessment survey - answered in full, saved to retrospectives
10. Post-run metrics - post_run_metrics.py run, metric event emitted to bus
11. Loop report written - before/after scores, metrics, survey summaries, artifacts updated
12. Test registry updated - scenario file closed with results, registry.md row updated, recommended next test written
13. Improvement ledger updated - Section 1 eval row + Section 2 survey-action rows appended; optional chart regenerated
14. L2/L3 memory promotion - session-memory-manager runs, facts promoted with dedup IDs
15. os-learning-loop trigger check - if 3+ friction events of same type OR north star regressing 2+ sessions, trigger Full Loop

Emitting eval.result with score:0.9 is not a completed loop. The survey, the metrics, and
the memory write are the loop. The signal is just coordination glue.

---

## Stage 1: Setup and Orientation

Goal: Every agent orients before any work begins. No agent starts cold.

1. ORCHESTRATOR reads (in order):
   - context/memory/improvement-ledger.md - eval score trajectory per skill, survey-to-action trace, north star trend
   - context/memory/tests/registry.md - what has been tested, what was recommended next
   - context/memory.md (L3 long-term facts)
   - Last session log: context/memory/YYYY-MM-DD.md
   - Last retrospective surveys: context/memory/retrospectives/ (most recent per agent)
   - context/events.jsonl last 100 lines for friction patterns from prior cycle
2. ORCHESTRATOR answers before writing any strategy packet:
   - What does the improvement ledger show for this target's score trajectory?
   - Is the north star completion rate regressing 2+ sessions in a row? (if yes, trigger os-learning-loop before this cycle)
   - What does the test registry say was the recommended next test?
   - Has this hypothesis already been confirmed or falsified? (check registry - do not re-run)
   - Which survey friction items from prior cycles have not been acted on yet? (Section 2 gaps)
3. Confirm agents.json lists all participating agents.
4. Each agent emits agent_start.
5. ORCHESTRATOR documents the test scenario in context/memory/tests/[CYCLE_ID]_[TARGET_SLUG].md
   per references/test-registry-protocol.md - BEFORE emitting loop.start.
6. Add row to context/memory/tests/registry.md with status IN PROGRESS.
7. ORCHESTRATOR emits loop.start.

---

## Stage 4: Mandatory Loop Close (Every Cycle - No Exceptions)

### 4.2 Agent Self-Assessment Survey (Each Agent)

Every agent that performed work this cycle MUST complete the Post-Run Self-Assessment Survey.
Save to: context/memory/retrospectives/survey_[YYYYMMDD]_[HHMM]_[AGENT].md

Survey sections (all mandatory):
- Run Metadata: date, task type, complexity, skill under test
- Completion Outcome: full workflow end to end? major human rescue required?
- Count-Based Signals: times uncertain, steps missed, wrong CLI, redirected by human, total friction
- Qualitative Friction: 6 questions about uncertainty, ambiguity, confusion, friction, avoidable failures
- Improvement Recommendation: one change to test before next run, evidence, target file

### 4.3 Run Post-Run Metrics

```bash
python3 "${CLAUDE_PLUGIN_ROOT}/hooks/scripts/post_run_metrics.py"
```

Emits a metric event with: human_interventions, workflow_uncertainty, missed_steps,
cli_errors, friction_events_total, hook_errors.

### 4.5 Loop Report (Every Cycle)

ORCHESTRATOR writes to context/memory/loop-reports/report_[CYCLE_ID].md.
Sections: Agent Summaries, Baseline vs Result, Survey Response Summary, Artifacts Updated.
Never auto-display - ask user if they want to see it.

### 4.6 Test Registry Update (MANDATORY - Every Cycle)

Close the scenario file with results (eval scores, metrics, survey findings, hypothesis outcome,
what not covered, recommended next test). Update registry.md row to CLOSED.
Confirmed findings -> L3 memory. Falsified -> DO NOT RE-TEST entry.

### 4.7 Update Improvement Ledger (Every Cycle - No Exceptions)

Append to context/memory/improvement-ledger.md - see references/improvement-ledger-spec.md.

Section 1 - one row per cycle (KEEP or DISCARD):
| [DATE] | [CYCLE_ID] | [TARGET] | [baseline] | [after] | [delta] | KEEP/DISCARD | [sub-cycles] | [change summary] |

Section 2 - one row per survey friction item that generated a change attempt:
| [DATE] | [survey ID] | [AGENT] | [friction item exact quote] | [action] | [file] | [change] | [eval delta] | KEEP/DISCARD/pending |

Section 3 - one row per session (written ONCE at session close):
| [DATE] | [session] | [total cycles] | [completed without rescue] | [rate %] | [interventions] | [friction total] | [trend] |

Optional: update progress chart after every KEEP cycle:
```bash
python3 "${CLAUDE_PLUGIN_ROOT}/skills/loop-progress-report/scripts/generate_report.py" \
  --project-dir "${CLAUDE_PROJECT_DIR}" \
  --plugin-dir "${CLAUDE_PLUGIN_ROOT}"
```

If north star regresses 2 consecutive sessions: invoke os-learning-loop in Full Loop mode.

### 4.8 Promote to Long-Term Memory

Run session-memory-manager.

### 4.10 os-learning-loop Trigger Check

If any single friction cause appears 3+ times this cycle: invoke os-learning-loop Full Loop automatically.

### 4.11 Release Locks and Shutdown

```bash
python3 "$KERNEL_PY" release_lock memory
```

---

## North Star Metric

Autonomous Workflow Completion Rate: percentage of cycles completing the full chain
without human rescue. Two consecutive session regressions = mandatory os-learning-loop trigger.

---

## References

- loop-progress-report SKILL: ../loop-progress-report/SKILL.md
- improvement-ledger-spec.md: ../../references/improvement-ledger-spec.md
- test-registry-protocol.md: ../../references/test-registry-protocol.md
- kernel.py: ../agentic-os-init/runtime/kernel.py
```

---

## File: `plugins/agent-agentic-os/references/improvement-ledger-spec.md`
> Note: NEW - defines the longitudinal improvement ledger at context/memory/improvement-ledger.md. Three sections: eval score progression (KEEP/DISCARD per cycle), survey-to-action trace (friction item to file change to score delta), north star completion rate per session. This is the file that makes "is this skill getting better over 10 cycles?" answerable in one read.

```markdown
# Improvement Ledger Specification

The improvement ledger is the longitudinal record of the flywheel. Individual loop
reports, surveys, and test registry entries are point-in-time snapshots. The ledger
is the accumulation - it answers the questions that per-cycle files cannot:

- Is eval score for session-memory-manager trending up or down over 10 cycles?
- Which survey friction item led to which skill change, and did the change improve the score?
- What is the Autonomous Workflow Completion Rate this month vs last month?

## Location

context/memory/improvement-ledger.md

Read at every ORCHESTRATOR orientation (Stage 1). Written at every loop close (Stage 4.7).
Never deleted - only appended.

---

## Section 1: Eval Score Progression

One row per KEEP or DISCARD cycle per skill target. Written by ORCHESTRATOR at Stage 4.7.

| Date | Cycle ID | Target | Baseline | After | Delta | Verdict | Sub-cycles to KEEP | Change Summary |
|------|----------|--------|----------|-------|-------|---------|-------------------|----------------|
| 2026-03-21 | cycle-20260321-001 | session-memory-manager | 0.00 (first run) | 0.72 | +0.72 | KEEP | 1 | Added Phase 3 test registry preservation |
| 2026-03-22 | cycle-20260322-001 | session-memory-manager | 0.72 | 0.78 | +0.06 | KEEP | 2 | Tightened dedup conflict detection wording |
| 2026-03-23 | cycle-20260323-001 | session-memory-manager | 0.78 | 0.76 | -0.02 | DISCARD | 3 | Reverted - adversarial prompt change degraded routing |

Rules:
- DISCARD cycles are also recorded (they show what did NOT work).
- Baseline for first run: "0.00 (first run)" - it establishes the baseline.
- Sub-cycles to KEEP counts how many INNER_AGENT attempts before KEEP verdict.
- Do NOT summarize - record the actual section name or line changed.

---

## Section 2: Survey-to-Action Trace

The accountability chain from friction to fix to outcome. One row per survey finding
that resulted in a concrete change attempt.

| Date | Survey ID | Agent | Friction Item | Action Taken | Target File | Change Made | Eval Delta | Outcome |
|------|-----------|-------|---------------|--------------|-------------|-------------|------------|---------|
| 2026-03-21 | survey_20260321_1030_INNER_AGENT | INNER_AGENT | "eval_runner.py --skill flag not documented" | Added --skill flag examples | skills/skill-improvement-eval/SKILL.md | Added code block with --skill flag | +0.06 | KEEP |
| 2026-03-22 | survey_20260322_1145_PEER_AGENT | PEER_AGENT | "dedup protocol ambiguous for paraphrased facts" | Rewrote Phase 4 dedup instruction | skills/session-memory-manager/SKILL.md | Phase 4 step 2 rewritten with example | +0.06 | KEEP |

Rules:
- Only record friction items where an action was taken.
- Eval Delta = score change in next eval on same target AFTER the change.
- This trace is how you prove the flywheel is working: friction -> change -> score delta -> keep.
- One row per friction item that generated a change. One survey can generate multiple rows.

---

## Section 3: North Star Metric Log

One row per session.

| Date | Session | Total Cycles | Completed Without Human Rescue | Completion Rate | Human Interventions | Friction Events Total | Trend |
|------|---------|-------------|-------------------------------|----------------|--------------------|-----------------------|-------|
| 2026-03-21 | session-001 | 2 | 1 | 50% | 3 | 7 | (baseline) |
| 2026-03-22 | session-002 | 3 | 2 | 67% | 2 | 4 | +17% |
| 2026-03-23 | session-003 | 3 | 3 | 100% | 0 | 1 | +33% |

Rules:
- Written ONCE per session at session close - not per cycle.
- Two consecutive regressions = mandatory os-learning-loop Full Loop trigger.

---

## Writing Protocol

ORCHESTRATOR writes at Stage 4.7, after test registry update (4.6), before memory promotion (4.8).

1. Read the current improvement-ledger.md.
2. Append new rows - never rewrite existing rows.
3. Verify the append with Read (Post-Write Verification protocol).

---

## Initialization

```bash
cat > context/memory/improvement-ledger.md << 'EOF'
# Improvement Ledger

## Eval Score Progression

| Date | Cycle ID | Target | Baseline | After | Delta | Verdict | Sub-cycles to KEEP | Change Summary |
|------|----------|--------|----------|-------|-------|---------|-------------------|----------------|

## Survey-to-Action Trace

| Date | Survey ID | Agent | Friction Item | Action Taken | Target File | Change Made | Eval Delta | Outcome |
|------|-----------|-------|---------------|--------------|-------------|-------------|------------|---------|

## North Star Metric: Autonomous Workflow Completion Rate

| Date | Session | Total Cycles | Completed Without Human Rescue | Completion Rate | Human Interventions | Friction Events Total | Trend |
|------|---------|-------------|-------------------------------|----------------|--------------------|-----------------------|-------|
EOF
```

---

## Anti-Patterns

- Rewriting rows: Never modify a prior row. Append-only.
- Vague change summaries: "Improved wording" is not a change summary. Write the actual section changed.
- Not recording DISCARDs: Negative results are as important as positive ones.
- Skipping Section 2: Only writing Section 1 breaks the causal chain.
- Writing Section 3 per cycle instead of per session.
```

---

## File: `plugins/agent-agentic-os/references/test-registry-protocol.md`
> Note: NEW - protocol requiring every test cycle to be documented BEFORE running (hypothesis, acceptance/failure criteria, prior results consulted). Results recorded after (score delta, survey findings, recommended next test). Confirmed findings promoted to L3. Falsified hypotheses get DO NOT RE-TEST entries preventing future wasted cycles.

```markdown
# Test Registry Protocol

Every loop cycle MUST be documented in the test registry. The registry is how the OS learns
which tests revealed useful information and how to design better tests over time.

## Registry Locations

context/memory/tests/
  registry.md                           <- index of all test scenarios
  [CYCLE_ID]_[TARGET_SLUG].md           <- one file per cycle with full scenario + results

---

## Before the Test: Document the Scenario

ORCHESTRATOR MUST write the test scenario record BEFORE emitting task.assigned.

Template:

# Test Scenario: [CYCLE_ID] - [TARGET_SLUG]

## Design
- Target: [skill/script/hook/workflow being tested]
- Hypothesis: [what change or question is this test designed to answer]
- Why this target now: [what prior result, survey finding, or friction pattern motivated this]
- What prior tests told us: [reference to relevant prior cycles in registry.md]
- Acceptance criteria: [what result would confirm the hypothesis - be specific]
- Failure criteria: [what result would falsify it]
- Known weaknesses in this test design: [limitations acknowledged upfront]

## Status: IN PROGRESS

---

## After the Test: Record Results

## Results

### Eval Scores
- Baseline: [score from results.tsv]
- After: [new score]
- Delta: [+/-]
- Verdict: KEEP / DISCARD

### Metrics
- Friction events this cycle: N
- Human interventions: N
- Cycles to KEEP: N

### Survey Findings
- INNER_AGENT headline friction: [one line]
- PEER_AGENT headline friction: [one line]
- Shared friction pattern (if any): [same issue cited by 2+ agents]

### Hypothesis Outcome
- Confirmed / Falsified / Inconclusive
- Evidence: [cite specific score delta, survey excerpt, or event]

### What This Test Did Not Cover
- [known gaps in what was measured]

### Recommended Next Test
- Target: [same or different target]
- Hypothesis: [what to test next, directly informed by these results]
- Design improvement: [one specific change to make the next test more discriminating]

## Status: CLOSED - [VERDICT]

---

## Registry Index Format

# Test Registry

| Cycle ID | Target | Date | Hypothesis | Status | Verdict |
|----------|--------|------|------------|--------|---------|
| cycle-20260321-001 | session-memory-manager | 2026-03-21 | Phase 5 survey addition improves routing | CLOSED | KEEP |

---

## Promotion Rules

- registry.md index row: always preserved (never archived, always in L3)
- Closed scenario files: preserved in context/memory/tests/ for 90 days, then archived. Never deleted.
- Confirmed findings: promote to context/memory.md L3 with dedup ID referencing the cycle ID
- Falsified hypotheses: record in L3 as "DO NOT RE-TEST: [hypothesis] - falsified by [CYCLE_ID]"

---

## Anti-Patterns

- Testing without a prior-results check
- Hypothesis-free testing: "Let's just run it and see" produces uninterpretable results
- Not recording falsified results
- Closing without a recommended next test
```

---

## File: `plugins/agent-agentic-os/references/test-scenarios-seed.md` (abridged - index + T01-T12)
> Note: NEW - 50 pre-designed test hypotheses covering all skills, scripts, agents, hooks. ORCHESTRATOR reads at orientation and selects untested scenarios. Every scenario has acceptance criteria, failure criteria, known weaknesses, and recommended next test chain. All results.tsv baselines currently empty - first run of each skill establishes baseline. Full file contains T01-T50; this bundle shows the index and T01-T12.

```markdown
# Test Scenario Bank - Agentic OS Plugin

Pre-designed hypotheses for the test registry. ORCHESTRATOR reads this at orientation
and selects the highest-priority untested scenario for each cycle.

Status note: All results.tsv baselines are empty. First eval run establishes baseline.

---

## Registry Index (All 50 Scenarios)

| ID | Target | Hypothesis | Priority |
|----|--------|------------|----------|
| T01 | skill-improvement-eval | Baseline routing: eval triggers on correct phrases, not on explain/describe | HIGH |
| T02 | skill-improvement-eval | KEEP/DISCARD threshold: clearly improved skill scores KEEP, degraded scores DISCARD | HIGH |
| T03 | skill-improvement-eval | Adversarial coverage: eval catches prompts designed to bypass routing | MEDIUM |
| T04 | skill-improvement-eval | Edge case: graceful failure when evals.json is missing or malformed | MEDIUM |
| T05 | skill-improvement-eval | Survey completion: evaluator completes self-assessment after every eval run | HIGH |
| T06 | concurrent-agent-loop | Baseline 1-cycle smoke: all 14 mandatory loop steps execute in order | HIGH |
| T07 | concurrent-agent-loop | Friction emission: agents emit type:friction events when encountering ambiguity | HIGH |
| T08 | concurrent-agent-loop | Loop report written: report file created after every cycle regardless of outcome | HIGH |
| T09 | concurrent-agent-loop | Survey completion: both ORCHESTRATOR and INNER_AGENT complete survey after cycle | HIGH |
| T10 | concurrent-agent-loop | Memory persistence: at least one promoted fact in context/memory.md after cycle | MEDIUM |
| T11 | concurrent-agent-loop | Auto-trigger: os-learning-loop triggered when friction_events_total reaches 3 | HIGH |
| T12 | concurrent-agent-loop | Lock contention: second agent attempting same lock receives clear failure, not hang | HIGH |
| T13 | os-clean-locks | Baseline routing: triggers on /os-clean-locks and lock variants | HIGH |
| T14 | os-clean-locks | Stale lock cleanup: lock with dead PID gets removed | HIGH |
| T15 | os-clean-locks | Active lock protection: lock held by running PID is preserved | HIGH |
| T16 | os-clean-locks | Post-clean verification: state_update clears active_agent field after cleanup | MEDIUM |
| T17 | os-clean-locks | Survey completion: os-clean-locks completes self-assessment after every run | MEDIUM |
| T18 | session-memory-manager | Baseline promotion: ephemeral state skipped, architectural decisions promoted | HIGH |
| T19 | session-memory-manager | Dedup detection: semantic duplicate caught and flagged before write | HIGH |
| T20 | session-memory-manager | Test registry preservation: registry.md skipped by archive logic, never deleted | HIGH |
| T21 | session-memory-manager | Conflict detection: CONFLICT marker emitted before any write with overlap | HIGH |
| T22 | session-memory-manager | Size enforcement: memory.md over 50KB triggers archive, not silent truncation | MEDIUM |
| T23 | session-memory-manager | Survey completion: memory manager completes self-assessment after every session | HIGH |
| T24 | session-memory-manager | Rollback protocol: rejected write triggers git stash pop, file reverted | MEDIUM |
| T25 | session-memory-manager | Falsified hypothesis: DO NOT RE-TEST entry added to memory.md when hypothesis falsified | HIGH |
| T26 | learning-loop | Baseline 3-phase completion: observe, hypothesize, test phases all execute | HIGH |
| T27 | learning-loop | Survey Phase V: survey completed and saved to retrospectives/ after every run | HIGH |
| T28 | learning-loop | Metrics Phase VI: post_run_metrics.py fires and emits metric event | HIGH |
| T29 | learning-loop | Memory Phase VII: session-memory-manager invoked before handoff | HIGH |
| T30 | learning-loop | Handoff gate: Phase VIII only starts after survey + metrics + memory | HIGH |
| T31 | dual-loop | Baseline handoff: outer loop assigns task, inner loop completes and signals back | HIGH |
| T32 | dual-loop | Survey both agents: outer AND inner loop both complete self-assessment | HIGH |
| T33 | dual-loop | Friction propagation: inner loop friction events visible to outer loop via event bus | MEDIUM |
| T34 | dual-loop | Result surfacing: inner loop eval result surfaced to outer loop before next iteration | HIGH |
| T35 | dual-loop | Auto-trigger: os-learning-loop flagged when same friction cause appears 3+ times | HIGH |
| T36 | os-learning-loop | Registry orientation: Phase 1 reads registry.md and lists prior confirmed/falsified | HIGH |
| T37 | os-learning-loop | DO NOT RE-TEST respected: agent skips hypothesis already falsified | HIGH |
| T38 | os-learning-loop | Pre-eval documentation: scenario file written BEFORE eval_runner.py is called | HIGH |
| T39 | os-learning-loop | Registry close: registry.md row updated from IN PROGRESS to CLOSED after cycle | HIGH |
| T40 | os-learning-loop | Fast Path vs Full Loop: Fast Path halts before Phase 3, does not acquire lock | MEDIUM |
| T41 | kernel.py | PROTECTED_STATE_KEYS state_update guard: execution_mode rejects string overwrite | HIGH |
| T42 | kernel.py | PROTECTED_STATE_KEYS state_increment guard: execution_mode rejects integer increment (round 8 regression test) | HIGH |
| T43 | kernel.py | Stale lock auto-cleanup: lock file with dead PID cleared automatically on next acquire | HIGH |
| T44 | kernel.py | Lock idempotency: same agent cannot acquire the same lock twice without releasing | MEDIUM |
| T45 | kernel.py | Agent permit list: emit_event from unlisted agent name is rejected | HIGH |
| T46 | hooks | Friction count: post_run_metrics.py correctly counts type:friction events | HIGH |
| T47 | hooks | Metrics on Stop: metrics report written to disk on every Stop hook fire | HIGH |
| T48 | hooks | Auto-trigger threshold: friction_events_total >= 3 triggers os-learning-loop | HIGH |
| T49 | agentic-os-init | Interview skip: init does not repeat questions already answered | MEDIUM |
| T50 | agentic-os-init | Survey completion: init completes self-assessment after every onboarding run | HIGH |

---

## Scenario Details (T01-T12; T13-T50 in full file)

### T01 - skill-improvement-eval: Baseline Routing Accuracy
- Hypothesis: Triggers on "evaluate this skill", NOT on "explain this skill".
- Acceptance criteria: Triggers on 3/3 positive phrases; does NOT trigger on 3/3 negative.
- Failure criteria: Any false positive or false negative routing.
- Known weaknesses: Routing inferred from description text, not a real execution test.
- Recommended next test: T02 (KEEP/DISCARD threshold).

### T02 - skill-improvement-eval: KEEP/DISCARD Threshold
- Hypothesis: Correctly assigns KEEP to skill with added positive example; DISCARD to skill with removed keyword.
- Acceptance criteria: KEEP for +1 positive example; DISCARD for -1 keyword.
- Failure criteria: KEEP/DISCARD swapped, or BASELINE returned for both.
- Known weaknesses: eval_runner.py may return BASELINE if score delta is within noise band.
- Recommended next test: T03 (adversarial coverage).

### T03 - skill-improvement-eval: Adversarial Prompt Coverage
- Hypothesis: evals.json catches a skill manipulated to trigger on unrelated keywords.
- Acceptance criteria: DISCARD returned when skill patched with unrelated trigger keywords.
- Failure criteria: KEEP returned despite adversarial manipulation.
- Recommended next test: Add 2 adversarial prompts to evals.json and re-run T02.

### T04 - skill-improvement-eval: Graceful Failure on Missing evals.json
- Hypothesis: eval_runner.py exits with a clear error message (not a Python traceback) when evals.json is absent.
- Acceptance criteria: Exit code non-zero, stderr contains human-readable error, no traceback.
- Failure criteria: Python traceback to stdout, or silent exit code 0 on failure.
- Recommended next test: Test with truncated JSON to check partial corruption handling.

### T05 - skill-improvement-eval: Self-Assessment Survey Completion
- Hypothesis: Evaluator saves a survey to retrospectives/ after every eval run, including DISCARD runs.
- Acceptance criteria: File retrospectives/survey_[DATE]_[TIME]_skill-improvement-eval.md exists with all qualitative sections populated.
- Failure criteria: File missing, or sections left blank.
- Recommended next test: Read survey and check if Improvement Recommendation is actionable.

### T06 - concurrent-agent-loop: Baseline 1-Cycle Smoke Test
- Hypothesis: A single loop cycle completes all 14 mandatory steps.
- Acceptance criteria: All 14 steps logged in events.jsonl; loop report written to disk.
- Failure criteria: Any mandatory step absent from event stream.
- Recommended next test: T07 (friction events) using same test harness.

### T07 - concurrent-agent-loop: Friction Event Emission
- Hypothesis: When INNER_AGENT encounters an ambiguous task, it emits type:friction before requesting human rescue.
- Acceptance criteria: At least 1 type:friction event in events.jsonl during the cycle.
- Failure criteria: Agent requests human rescue without a prior friction event.
- Recommended next test: T11 (auto-trigger at 3 friction events).

### T08 - concurrent-agent-loop: Loop Report Written Every Cycle
- Hypothesis: A loop report is written after every cycle, including DISCARD cycles.
- Acceptance criteria: Report file exists with all 4 sections.
- Failure criteria: File missing, or only written on KEEP cycles.
- Recommended next test: Review report quality against the 4-section template.

### T09 - concurrent-agent-loop: Survey Completion by Both Agents
- Hypothesis: Both ORCHESTRATOR and INNER_AGENT complete and save self-assessment surveys after every cycle.
- Acceptance criteria: Two survey files in retrospectives/ after 1 cycle.
- Failure criteria: Only 1 survey file, or no survey files.
- Recommended next test: T35 (dual-loop survey) in proper 2-session setup.

### T10 - concurrent-agent-loop: Memory Persistence
- Hypothesis: At least one fact promoted to context/memory.md after every KEEP cycle.
- Acceptance criteria: memory.md has a new entry with today's date after loop close.
- Failure criteria: memory.md unchanged after a KEEP cycle.
- Recommended next test: T19 (session-memory-manager dedup detection).

### T11 - concurrent-agent-loop: os-learning-loop Auto-Trigger
- Hypothesis: os-learning-loop invoked automatically when friction_events_total reaches 3 in a single cycle.
- Acceptance criteria: Full Loop starts without explicit user request when 3+ friction events of same type detected.
- Failure criteria: Loop ends without triggering os-learning-loop despite friction threshold crossed.
- Recommended next test: T48 (hooks auto-trigger threshold verification).

### T12 - concurrent-agent-loop: Lock Contention
- Hypothesis: When two agents attempt the same lock simultaneously, the second gets a clear failure, not a hang.
- Acceptance criteria: Second acquire_lock prints "[Kernel] Lock busy: name" to stderr and exits 1 within 5 seconds.
- Failure criteria: Second agent hangs indefinitely, or acquires the lock silently.
- Recommended next test: T43 (kernel.py stale lock auto-cleanup).

*T13-T50 details are in the full file at references/test-scenarios-seed.md*
*T42 is the permanent regression test for the round 8 state_increment PROTECTED_STATE_KEYS bug.*
```

---

## File: `plugins/agent-agentic-os/skills/loop-progress-report/SKILL.md`
> Note: NEW skill - generates autoresearch-style progress chart from per-skill results.tsv files and improvement-ledger.md. Green KEEP dots, gray DISCARD dots, running-best step line, annotations showing what each improvement was. Works cross-plugin (agent-agentic-os and exploration-cycle-plugin). Dependencies: pandas, matplotlib at skill root requirements.in/requirements.txt.

```markdown
---
name: loop-progress-report
description: >
  Trigger with "show me the improvement chart", "how are we improving", "progress report",
  "graph the eval scores", "show cycle of improvement", "what's the trend", "are we getting
  better", "show improvement over time", "generate the progress chart", or any time the user
  wants a visual or text summary of how the agentic loop is improving across cycles.
  Works across all plugins that write to an improvement-ledger.md.
allowed-tools: Bash, Read, Write
---

## Dependencies

Requires Python 3.8+, pandas, and matplotlib.

```bash
pip-compile ./requirements.in
pip install -r ./requirements.txt
```

See ./requirements.txt for the dependency lockfile.

---

# Loop Progress Report

Visual and text reporting on the agentic loop improvement cycle. The reference output is
the autoresearch progress chart: green KEEP dots on a timeline, gray DISCARD dots,
running-best step line, annotations showing what each improvement was.

## What It Reads

| Source | Content |
|--------|---------|
| plugins/*/skills/*/evals/results.tsv | Per-skill eval score history (primary source, same format as autoresearch) |
| context/memory/improvement-ledger.md | Survey-to-action trace, north star metric (secondary) |

## What It Produces

| Output | Description |
|--------|-------------|
| context/memory/reports/progress_YYYYMMDD_HHMM.png | Progress chart: KEEP/DISCARD timeline, running-best step line, annotations |
| context/memory/reports/summary_YYYYMMDD_HHMM.md | Text summary: top hits by delta, north star trend |

## Execution Flow

### Phase 1: Check data availability

If ledger exists but Section 1 table is empty, inform user no cycles completed yet.
Do not run report script on empty data - it will produce an empty chart.

### Phase 2: Run the report

```bash
PLUGIN_DIR="${CLAUDE_PLUGIN_ROOT:-$(pwd)/plugins/agent-agentic-os}"
PROJECT_DIR="${CLAUDE_PROJECT_DIR:-$(pwd)}"

python3 "${PLUGIN_DIR}/skills/loop-progress-report/scripts/generate_report.py" \
  --project-dir "$PROJECT_DIR" \
  --plugin-dir "$PLUGIN_DIR"
```

### Phase 3: Surface the output

Report the chart path. Print the text summary inline. Ask: "Would you like me to open the chart?"

### Phase 4: Cross-plugin reporting (optional)

Run twice with different --project-dir - once for agent-agentic-os, once for exploration-cycle-plugin.

## Reading the Chart

- Gray dots: DISCARD cycles - attempts that did not improve the skill
- Green dots: KEEP cycles - improvements that stuck
- Green step line: Running best - the frontier of improvement over time
- Annotations: What change was made on each KEEP cycle

Flat or declining step line = the loop is not improving the skill.
Frequent DISCARD clusters = hypothesis quality needs work (check test scenarios seed).
Steep step-line rises = the survey-to-action trace is working.

## References

- improvement-ledger-spec.md: ../../references/improvement-ledger-spec.md
- concurrent-agent-loop SKILL: ../concurrent-agent-loop/SKILL.md (Stage 4.7 writes to the ledger)
- test-scenarios-seed.md: ../../references/test-scenarios-seed.md
```

---

## File: `plugins/agent-agentic-os/skills/loop-progress-report/scripts/generate_report.py`
> Note: NEW - reads per-skill results.tsv files as primary data source (same format as autoresearch), improvement-ledger.md north star section as secondary. Produces autoresearch-style progress.png: gray DISCARD dots, green KEEP dots, running-best step line (computed as max(scores[:i+1]) for each i), annotations with change_summary per KEEP dot. Also produces text summary with top hits sorted by delta.

```python
"""
loop_progress_report.generate_report
-------------------------------------
Reads per-skill results.tsv files (same format as autoresearch) and the improvement ledger
to produce progress charts and a text summary.

Per-skill TSV format (tab-separated):
    cycle_id  score  status  change_summary
    (status: KEEP / DISCARD / BASELINE)

Output:
    context/memory/reports/progress_YYYYMMDD_HHMM.png   -- one subplot per skill + north star
    context/memory/reports/summary_YYYYMMDD_HHMM.md     -- text summary

Usage:
    python3 generate_report.py --plugin-dir /path/to/agent-agentic-os [--project-dir /path/to/project]

Dependencies: pandas, matplotlib (see requirements.txt)
"""

import argparse
import re
import sys
from datetime import datetime
from pathlib import Path

try:
    import matplotlib.pyplot as plt
    import matplotlib.gridspec as gridspec
    import pandas as pd
except ImportError as e:
    print(f"[ERROR] Missing dependency: {e}")
    print("Run: pip install -r requirements.txt")
    sys.exit(1)


KEEP_COLOR    = "#2ecc71"
DISCARD_COLOR = "#cccccc"
LINE_COLOR    = "#27ae60"


# ---------------------------------------------------------------------------
# Data loading - results.tsv (primary, per-skill, like autoresearch)
# ---------------------------------------------------------------------------

def find_results_tsvs(plugin_dir: Path) -> dict[str, Path]:
    """Return {skill_name: path_to_results.tsv} for every non-empty results.tsv."""
    found: dict[str, Path] = {}
    for tsv in plugin_dir.glob("skills/*/evals/results.tsv"):
        if tsv.stat().st_size > 0:
            found[tsv.parent.parent.name] = tsv
    # Also check skills/*/results.tsv (alternate location)
    for tsv in plugin_dir.glob("skills/*/results.tsv"):
        skill_name = tsv.parent.name
        if skill_name not in found and tsv.stat().st_size > 0:
            found[skill_name] = tsv
    return found


def load_results_tsv(path: Path, skill_name: str) -> pd.DataFrame:
    """
    Load a results.tsv. Expected columns (flexible):
      cycle_id | score | status | change_summary
    Falls back gracefully if columns differ.
    """
    try:
        df = pd.read_csv(path, sep="\t", dtype=str)
    except Exception as e:
        print(f"[warn] Could not read {path}: {e}")
        return pd.DataFrame()

    df.columns = [c.strip().lower().replace(" ", "_") for c in df.columns]

    # Normalise the score column (accept: score, val_bpb, eval_score)
    for alias in ("val_bpb", "eval_score", "bpb"):
        if alias in df.columns and "score" not in df.columns:
            df = df.rename(columns={alias: "score"})

    # Normalise the status column
    if "status" in df.columns:
        df["status"] = df["status"].str.strip().str.upper()
    else:
        df["status"] = "UNKNOWN"

    if "score" in df.columns:
        df["score"] = pd.to_numeric(df["score"], errors="coerce")

    # Normalise description / change_summary
    for alias in ("description", "change_summary", "change", "summary"):
        if alias in df.columns and "change_summary" not in df.columns:
            df = df.rename(columns={alias: "change_summary"})

    if "change_summary" not in df.columns:
        df["change_summary"] = ""

    df["skill"] = skill_name
    return df


# ---------------------------------------------------------------------------
# Ledger loading (secondary - for north star + survey trace)
# ---------------------------------------------------------------------------

def _parse_md_table(section_text: str) -> pd.DataFrame:
    lines = [l.strip() for l in section_text.strip().splitlines() if l.strip()]
    header: list[str] | None = None
    rows: list[list[str]] = []
    for line in lines:
        if not line.startswith("|"):
            continue
        cells = [c.strip() for c in line.strip("|").split("|")]
        if all(re.match(r"^-+$", c.replace(" ", "")) for c in cells if c):
            continue
        if header is None:
            header = cells
        else:
            rows.append(cells)
    if not header or not rows:
        return pd.DataFrame()
    rows = [r + [""] * (len(header) - len(r)) for r in rows]
    return pd.DataFrame(rows, columns=header)


def load_north_star(ledger_path: Path) -> pd.DataFrame:
    if not ledger_path.exists():
        return pd.DataFrame()
    text = ledger_path.read_text()
    m = re.search(r"## North Star Metric.*?\n(.*?)(?=\n## |\Z)", text, re.DOTALL)
    if not m:
        return pd.DataFrame()
    df = _parse_md_table(m.group(1))
    if df.empty:
        return df
    df.columns = [c.lower().replace(" ", "_") for c in df.columns]
    for col in ("total_cycles", "completed_without_human_rescue",
                "human_interventions", "friction_events_total"):
        if col in df.columns:
            df[col] = pd.to_numeric(df[col], errors="coerce")
    if "completion_rate" in df.columns:
        df["rate_num"] = pd.to_numeric(
            df["completion_rate"].str.rstrip("%"), errors="coerce"
        )
    return df


# ---------------------------------------------------------------------------
# Plotting - autoresearch style
# ---------------------------------------------------------------------------

def plot_skill(ax, df: pd.DataFrame, title: str) -> None:
    """Single skill progress chart - mirrors autoresearch analysis.ipynb."""
    if df.empty or "score" not in df.columns:
        ax.text(0.5, 0.5, "No data yet - run a loop cycle to populate results.tsv",
                ha="center", va="center", transform=ax.transAxes,
                fontsize=10, color="#aaaaaa")
        ax.set_title(title, fontsize=11)
        return

    df = df.reset_index(drop=True)

    kept    = df[df["status"] == "KEEP"]
    disc    = df[df["status"] == "DISCARD"]
    base    = df[df["status"] == "BASELINE"]

    # Discarded - faint grey dots
    if not disc.empty:
        ax.scatter(disc.index.tolist(), disc["score"].tolist(),
                   c=DISCARD_COLOR, s=12, alpha=0.5, zorder=2, label="Discarded")

    # Baseline - hollow dot
    if not base.empty:
        ax.scatter(base.index.tolist(), base["score"].tolist(),
                   c="white", s=40, zorder=3, edgecolors="#888888",
                   linewidths=1, label="Baseline")

    # Kept - green dots
    if not kept.empty:
        ax.scatter(kept.index.tolist(), kept["score"].tolist(),
                   c=KEEP_COLOR, s=55, zorder=4,
                   edgecolors="black", linewidths=0.5, label="Kept")

        # Running best step line (autoresearch style)
        scores = kept["score"].tolist()
        running_best = [max(scores[: i + 1]) for i in range(len(scores))]
        ax.step(kept.index.tolist(), running_best, where="post",
                color=LINE_COLOR, linewidth=2.0, alpha=0.75, zorder=3,
                label="Running best")

        # Annotate each kept point with what changed
        for idx, row in kept.iterrows():
            desc = str(row.get("change_summary", "")).strip()
            if len(desc) > 50:
                desc = desc[:47] + "..."
            if desc:
                ax.annotate(
                    desc, (idx, row["score"]),
                    textcoords="offset points",
                    xytext=(6, 6), fontsize=7.5,
                    color="#1a7a3a", alpha=0.9,
                    rotation=28, ha="left", va="bottom",
                )

    n_total = len(df)
    n_keep  = len(kept)
    n_disc  = len(disc)
    keep_rate = f"{n_keep / (n_keep + n_disc):.0%}" if (n_keep + n_disc) > 0 else "n/a"

    ax.set_title(f"{title}   ({n_total} cycles  |  {n_keep} kept  |  keep rate {keep_rate})",
                 fontsize=10)
    ax.set_xlabel("Cycle #", fontsize=9)
    ax.set_ylabel("Eval Score (higher = better)", fontsize=9)
    ax.legend(loc="lower right", fontsize=8)
    ax.grid(True, alpha=0.2)

    # Baseline dashed line
    if not df["score"].dropna().empty:
        first_score = df["score"].dropna().iloc[0]
        ax.axhline(first_score, color="#aaaaaa", linestyle="--", linewidth=0.8, alpha=0.6)
        ax.annotate(f"baseline {first_score:.4f}", (0, first_score),
                    textcoords="offset points", xytext=(4, 3),
                    fontsize=7, color="#999999")


def plot_north_star(ax, ns_df: pd.DataFrame) -> None:
    if ns_df.empty or "rate_num" not in ns_df.columns:
        ax.text(0.5, 0.5, "No session data yet",
                ha="center", va="center", transform=ax.transAxes,
                fontsize=11, color="#aaaaaa")
        ax.set_title("North Star: Autonomous Completion Rate", fontsize=10)
        return

    x = list(range(len(ns_df)))
    rate_strs: list[str] = ns_df["rate_num"].astype(str).tolist()
    y: list[float] = [float(s) if s not in ("", "nan", "None", "<NA>") else 0.0
                      for s in rate_strs]

    ax.plot(x, y, marker="o", color="#3498db", linewidth=2, markersize=7, zorder=3)
    ax.fill_between(x, y, alpha=0.15, color="#3498db")
    for xi, yi in zip(x, y):
        ax.annotate(f"{yi:.0f}%", (xi, yi),
                    textcoords="offset points", xytext=(0, 8),
                    fontsize=8, ha="center", color="#2980b9")
    ax.axhline(100, color="#e74c3c", linestyle="--", alpha=0.4, linewidth=1)
    ax.set_ylim(0, 115)
    sessions: list[str] = [ns_df.iloc[i].get("session", f"S{i+1}") for i in range(len(ns_df))]
    ax.set_xticks(x)
    ax.set_xticklabels(sessions, rotation=30, ha="right", fontsize=8)
    ax.set_ylabel("Completion Rate (%)", fontsize=9)
    ax.set_title("North Star: Autonomous Workflow Completion Rate", fontsize=10)
    ax.grid(True, alpha=0.2)


def plot_friction(ax, ns_df: pd.DataFrame) -> None:
    if ns_df.empty or "friction_events_total" not in ns_df.columns:
        ax.text(0.5, 0.5, "No friction data yet",
                ha="center", va="center", transform=ax.transAxes,
                fontsize=11, color="#aaaaaa")
        ax.set_title("Friction Events per Session (goal: decrease)", fontsize=10)
        return

    x = list(range(len(ns_df)))
    fric_strs: list[str] = ns_df["friction_events_total"].astype(str).tolist()
    y_vals: list[float] = [float(s) if s not in ("", "nan", "None", "<NA>") else 0.0
                           for s in fric_strs]
    ax.bar(x, y_vals, color="#e74c3c", alpha=0.65, zorder=3)
    for xi, yi in zip(x, y_vals):
        ax.text(xi, yi + 0.05, str(int(yi)), ha="center", va="bottom", fontsize=8)
    sessions: list[str] = [ns_df.iloc[i].get("session", f"S{i+1}") for i in range(len(ns_df))]
    ax.set_xticks(x)
    ax.set_xticklabels(sessions, rotation=30, ha="right", fontsize=8)
    ax.set_ylabel("Total Friction Events", fontsize=9)
    ax.set_title("Friction Events per Session (goal: decrease)", fontsize=10)
    ax.grid(True, alpha=0.2, axis="y")


# ---------------------------------------------------------------------------
# Summary text
# ---------------------------------------------------------------------------

def generate_summary(skill_frames: dict[str, pd.DataFrame],
                     ns_df: pd.DataFrame, date_str: str) -> str:
    lines = [f"# Loop Progress Report - {date_str}", ""]

    if not ns_df.empty and "rate_num" in ns_df.columns:
        latest = ns_df.iloc[-1]
        rate = latest.get("rate_num", "?")
        trend = latest.get("trend", "")
        lines += [f"## North Star: {rate}%  (trend: {trend})", ""]

    total_cycles = sum(len(df) for df in skill_frames.values())
    total_kept   = sum(len(df[df["status"] == "KEEP"]) for df in skill_frames.values()
                       if "status" in df.columns)
    lines += [
        f"## Overall: {total_cycles} cycles across {len(skill_frames)} skills, "
        f"{total_kept} kept improvements", ""
    ]

    for skill, df in skill_frames.items():
        if df.empty or "score" not in df.columns:
            continue
        kept = df[df["status"] == "KEEP"]
        disc = df[df["status"] == "DISCARD"]
        scores = df["score"].dropna()
        if scores.empty:
            continue

        baseline = scores.iloc[0]
        best     = kept["score"].max() if not kept.empty else baseline
        delta    = best - baseline

        lines.append(f"### {skill}")
        lines.append(f"- Cycles: {len(df)} total  |  {len(kept)} KEEP  |  {len(disc)} DISCARD")
        lines.append(f"- Baseline: {baseline:.4f}  ->  Best: {best:.4f}  (total improvement: {delta:+.4f})")

        if not kept.empty:
            lines.append("")
            lines.append("**Top improvements by delta:**")
            if len(kept) > 1:
                kept_sorted = kept.copy()
                prev_scores: list[float] = [baseline] + kept_sorted["score"].tolist()[:-1]
                kept_sorted["prev"] = prev_scores
                kept_sorted["delta"] = kept_sorted["score"] - kept_sorted["prev"]
                top = kept_sorted.sort_values(by="delta", ascending=False).head(5)  # type: ignore[call-overload]
            else:
                top = kept.copy()
                top["delta"] = kept["score"] - baseline

            for _, row in top.iterrows():
                d = float(row.get("delta") or 0)
                s = str(row.get("change_summary") or "")
                lines.append(f"  {d:+.4f}  {s}")
        lines.append("")

    return "\n".join(lines)


# ---------------------------------------------------------------------------
# Main
# ---------------------------------------------------------------------------

def main() -> None:
    parser = argparse.ArgumentParser(description="Agentic OS loop progress report")
    parser.add_argument("--plugin-dir", required=True,
                        help="Path to agent-agentic-os plugin root (contains skills/)")
    parser.add_argument("--project-dir", default=None,
                        help="Path to project root (for improvement-ledger.md)")
    parser.add_argument("--skill", default=None,
                        help="Filter to a single skill name")
    parser.add_argument("--output-dir", default=None,
                        help="Override output directory")
    args = parser.parse_args()

    plugin_dir  = Path(args.plugin_dir)
    project_dir = Path(args.project_dir) if args.project_dir else plugin_dir.parent
    ledger_path = project_dir / "context" / "memory" / "improvement-ledger.md"

    ts_str   = datetime.now().strftime("%Y%m%d_%H%M")
    date_str = datetime.now().strftime("%Y-%m-%d")

    out_dir = Path(args.output_dir) if args.output_dir else (
        project_dir / "context" / "memory" / "reports"
    )
    out_dir.mkdir(parents=True, exist_ok=True)

    # Load per-skill data from results.tsv files (primary source)
    tsvs = find_results_tsvs(plugin_dir)
    if args.skill:
        tsvs = {k: v for k, v in tsvs.items() if k.lower() == args.skill.lower()}

    skill_frames: dict[str, pd.DataFrame] = {}
    for skill_name, path in tsvs.items():
        df = load_results_tsv(path, skill_name)
        if not df.empty:
            skill_frames[skill_name] = df
            print(f"[loop-progress-report] Loaded {len(df)} rows from {path}")

    if not skill_frames:
        print("[loop-progress-report] No results.tsv data found. Run at least one eval cycle.")

    # Load north star from improvement ledger (secondary source)
    ns_df = load_north_star(ledger_path)

    # Figure layout - one subplot per skill + north star row
    n_skills  = max(1, len(skill_frames))
    n_rows    = n_skills + 1
    fig       = plt.figure(figsize=(16, 5 * n_rows))
    gs        = gridspec.GridSpec(n_rows, 2, figure=fig, hspace=0.60, wspace=0.35)

    for i, (skill_name, df) in enumerate(sorted(skill_frames.items())):
        ax = fig.add_subplot(gs[i, :])
        plot_skill(ax, df, title=skill_name.replace("-", " ").title())

    if not skill_frames:
        ax = fig.add_subplot(gs[0, :])
        plot_skill(ax, pd.DataFrame(), title="No skill data yet")

    ax_ns = fig.add_subplot(gs[n_rows - 1, 0])
    plot_north_star(ax_ns, ns_df)

    ax_fr = fig.add_subplot(gs[n_rows - 1, 1])
    plot_friction(ax_fr, ns_df)

    total_kept = sum(len(df[df["status"] == "KEEP"])
                     for df in skill_frames.values() if "status" in df.columns)
    total_cycles = sum(len(df) for df in skill_frames.values())

    fig.suptitle(
        f"Agentic OS - Loop Improvement Progress\n"
        f"{total_cycles} cycles  |  {total_kept} kept improvements  |  {date_str}",
        fontsize=14, y=1.01,
    )

    chart_path = out_dir / f"progress_{ts_str}.png"
    plt.savefig(chart_path, dpi=150, bbox_inches="tight")
    plt.close()
    print(f"[loop-progress-report] Chart saved: {chart_path}")

    summary = generate_summary(skill_frames, ns_df, date_str)
    summary_path = out_dir / f"summary_{ts_str}.md"
    summary_path.write_text(summary)
    print(f"[loop-progress-report] Summary saved: {summary_path}")
    print()
    print(summary)


if __name__ == "__main__":
    main()
```

---

## File: `plugins/agent-agentic-os/references/diagrams/loop-progress-report.mmd`
> Note: NEW sequence diagram - data flow from cycle execution through ledger write to chart generation. Shows all three ledger sections being written at Stage 4.7, and the optional chart update path after each KEEP cycle.

```
sequenceDiagram
    title Loop Progress Report - Data Flow

    participant ORCH as ORCHESTRATOR
    participant INNER as INNER_AGENT
    participant PEER as PEER_AGENT
    participant KERNEL as kernel.py
    participant LEDGER as improvement-ledger.md
    participant SURVEY as retrospectives/
    participant REPORT as generate_report.py
    participant CHART as progress_YYYYMMDD.png

    note over ORCH: Stage 1 - Orientation
    ORCH->>LEDGER: Read Section 1 (score trajectory for target)
    LEDGER-->>ORCH: Prior eval scores, KEEP/DISCARD history, running best
    ORCH->>LEDGER: Read Section 2 (unresolved survey friction items)
    LEDGER-->>ORCH: Prior friction items + whether each was resolved
    ORCH->>LEDGER: Read Section 3 (north star completion rate trend)
    LEDGER-->>ORCH: Completion rate per session, trend direction

    note over ORCH: Stage 2 - Execute cycle
    ORCH->>INNER: task.assigned (strategy packet informed by ledger)
    INNER->>KERNEL: emit friction events during work
    INNER->>PEER: eval result via kernel bus
    PEER->>PEER: run skill-improvement-eval independently
    PEER-->>ORCH: eval.result (KEEP/DISCARD + score delta)

    note over ORCH,PEER: Stage 4.2 - Surveys (both agents)
    INNER->>SURVEY: Save survey_[DATE]_INNER_AGENT.md
    PEER->>SURVEY: Save survey_[DATE]_PEER_AGENT.md
    ORCH->>SURVEY: Save survey_[DATE]_ORCHESTRATOR.md

    note over ORCH: Stage 4.6 - Test Registry Update
    ORCH->>ORCH: Close scenario file, update registry.md row

    note over ORCH: Stage 4.7 - Update Improvement Ledger
    ORCH->>LEDGER: Append Section 1 row (date, cycle_id, target, baseline, after, delta, verdict, change_summary)
    SURVEY-->>ORCH: Read friction items from all 3 surveys
    ORCH->>LEDGER: Append Section 2 rows (survey_id, agent, friction_item, action, file, change, eval_delta, outcome)
    KERNEL-->>ORCH: friction_events_total from post_run_metrics
    ORCH->>LEDGER: Append Section 3 row (session, completion_rate, human_interventions, friction_events_total)
    ORCH->>KERNEL: emit ledger_updated event

    note over ORCH: Optional chart update (after KEEP, or on user request)
    ORCH->>REPORT: python3 generate_report.py --project-dir ... --plugin-dir ...
    REPORT->>LEDGER: Parse Section 1 (eval score progression)
    REPORT->>CHART: Save progress_YYYYMMDD_HHMM.png
    REPORT-->>ORCH: Text summary (top hits by delta, completion rate trend)
    ORCH-->>User: Chart updated at reports/progress_[TS].png. Want to see the summary?

    note over ORCH: Next cycle orientation
    ORCH->>LEDGER: Read again - trajectory now includes this cycle's row
```

---

## File: `plugins/agent-agentic-os/agents/os-learning-loop.md`
> Note: UPDATED - Phase 1 now reads test registry AND improvement ledger before any analysis. Phase 3 requires test scenario documented BEFORE eval_runner.py is called. Phase 5 (new) closes the test registry entry with results. Phases renumbered: 6=Final Briefing, 7=Survey, 8=Closure. Auto-trigger condition added: friction_events_total >= 3 from post_run_metrics triggers Full Loop on next session start.

```markdown
---
name: os-learning-loop
description: >
  Trigger with "run the learning loop", "perform a retrospective", "how can we improve
  from this session", "update our skills based on what we learned", "conduct a post-run
  self-assessment", "complete the quality survey", or when the OS detects recurring friction.
model: inherit
color: magenta
tools: ["Bash", "Read", "Write"]
---

# OS Learning Loop Sub-Agent

Objective: Conduct a retrospective, identify inefficiencies, and permanently update the OS's
procedural memory (skills, CLAUDE.md) to continuously improve performance.

## Execution Modes

| Mode | When to use | Phases |
|------|-------------|--------|
| Fast Path | Routine session close, quick audit | 0, 1 (read only), 2, brief summary - stop before Phase 3 |
| Full Loop | User explicitly requests improvement, recurring friction, eval score regressed | All phases |

Auto-trigger condition: If post_run_metrics.py emits friction_events_total >= 3, the
os-learning-loop MUST run in Full Loop mode on the next session start.

---

### Phase 0: Intent Emission

```bash
python3 ${CLAUDE_PROJECT_DIR}/context/kernel.py emit_event \
  --agent os-learning-loop --type intent --action analyze_logs
```

### Phase 1: Context Gathering & OS State Lock

1. Update OS State (active_agent, mode, last_reflection).
2. Lock Protocol (Full Loop only): acquire_lock kernel. Fast Path = read-only, no lock.
3. Read Test Registry: Load context/memory/tests/registry.md.
   - Which targets already tested and what were the verdicts?
   - What was the "Recommended Next Test" from the most recent closed scenario?
   - Are there any "DO NOT RE-TEST" entries relevant to the current friction pattern?
4. Autonomous Friction Analysis: Read last 100 lines of context/events.jsonl and hook-errors.log.
5. Identify where the OS failed, stalled, or required too many turns.

### Phase 2: Root Cause Analysis

Determine the layer responsible:
- Kernel (CLAUDE.md): Missing global project context or build commands.
- RAM (context/): Outdated or conflicting facts in memory.md.
- Stdlib (skills/): A skill failed, had bad description (undertriggering), or needs creation.

### Phase 3: Action Execution

Check test registry first: Confirm hypothesis not already tested and falsified.

1. Design and propose a specific change.
2. Document the test scenario per references/test-registry-protocol.md BEFORE running eval:
   - Write context/memory/tests/[TIMESTAMP]_[TARGET_SLUG].md with hypothesis, acceptance/failure criteria.
   - Add IN PROGRESS row to context/memory/tests/registry.md.
3. Eval-Gate: Run eval_runner.py on proposed changes.
4. KEEP/DISCARD: Only present diff if KEEP or BASELINE. If DISCARD, revise and retry.
5. Create snapshot before applying Write (rollback safety).
6. Apply approved changes.

### Phase 4: Verification (Closed Loop)

Read before write. Conflict check. Safe Write Protocol. Post-Write Verification.
Validation tick: state_update validation_tick true.

### Phase 5: Test Registry Close

1. Update scenario file with Results section (eval delta, verdict, recommended next test).
2. Update registry.md row from IN PROGRESS to CLOSED.
3. If confirmed: promote finding to context/memory.md L3 with dedup ID.
4. If falsified: add "DO NOT RE-TEST" entry to context/memory.md.

### Phase 6: Final Briefing

Summarize what files were changed. Explain how OS will behave differently next time.

### Phase 7: Qualitative Self-Assessment Survey

Complete all sections of references/post_run_survey.md.
Save to: context/memory/retrospectives/survey_[DATE]_[TIME].md
Emit survey completion event.

### Phase 8: Closure & Lock Release

Emit result event. Execute release_lock kernel.
```

---

## File: `plugins/agent-agentic-os/skills/session-memory-manager/SKILL.md`
> Note: UPDATED - Phase 3 (new) preserves test registry artifacts before general promotion: registry.md is never archived, closed scenario files preserved 90 days, confirmed findings promoted to L3, falsified hypotheses get DO NOT RE-TEST entry. Phase 5 (new) mandatory self-assessment survey before releasing the lock. Previously this skill had no survey requirement.

```markdown
---
name: session-memory-manager
description: >
  Trigger with "remember this", "update memory", "what should we record from this session",
  "capture learnings", "write a session log", or when closing a session.
allowed-tools: Bash, Read, Write
---

# Session Memory Manager

Manages the three tiers of agent memory in an Agentic OS environment.

## Execution Flow

### Phase 0: Intent Emission

```bash
python3 context/kernel.py emit_event \
  --agent session-memory-manager --type intent --action promote_memory
```

### Phase 1: Acquire OS State and Lock

1. Update OS State (active_agent, mode, memory_gc_due).
2. Acquire lock: python3 context/kernel.py acquire_lock memory. If fails, abort.
3. Confirm session scope with user: main task, architectural decisions, bugs solved, open items.

### Phase 2: Write the Dated Session Log

Write to context/memory/YYYY-MM-DD.md using the standard template.

### Phase 3: Preserve Test Registry Artifacts

Before general promotion, handle test registry files specially:

1. context/memory/tests/registry.md - never archive, never skip. Always L3.
   Verify it exists and latest cycle row is CLOSED before proceeding.
2. Closed scenario files - preserve in place for 90 days, then move to archive/. Never delete.
3. Confirmed findings - if scenario has "Confirmed" hypothesis not yet in context/memory.md,
   promote it as a fact with cycle ID as evidence source.
4. Falsified hypotheses - check for "DO NOT RE-TEST" entry. If missing, add it now.

### Phase 4: Promote to Long-Term Memory

Ephemeral state -> SKIP. Open tasks -> SKIP. System facts/decisions -> PROMOTE.

Before any promotion:
1. Read context/memory.md and last 10 entries of MEMORY.md.
2. Search for semantic duplicates. Flag with CONFLICT before any Write.
3. Assign sequential dedup ID.
4. Safe Write Protocol (git stash + diff preview).
5. Post-Write Verification.

Enforce 50KB size limit on context/memory.md - archive if exceeded.

### Phase 5: Self-Assessment Survey (MANDATORY)

Before releasing the lock, complete the Post-Run Self-Assessment Survey.

Count-Based: uncertainty in promote/skip decisions, conflicts flagged, dedup IDs assigned.
Qualitative: which promotion felt most uncertain, was any fact promoted that should have been
skipped, which part of dedup protocol felt most ambiguous, one change to the session log template.

Save to: context/memory/retrospectives/survey_[YYYYMMDD]_[HHMM]_session-memory-manager.md

```bash
python3 context/kernel.py emit_event --agent session-memory-manager \
  --type learning --action survey_completed \
  --summary "retrospectives/survey_[DATE]_[TIME]_session-memory-manager.md"
```

### Phase 6: Confirm with User and Release Lock

Show completion summary. Emit success event. Release memory lock.
```

---

## File: `plugins/agent-agentic-os/skills/agentic-os-init/runtime/kernel.py`
> Note: BUG FIX from round 8 red-team review. state_increment was missing the PROTECTED_STATE_KEYS check that state_update had. An agent calling `state_increment --key execution_mode` would write integer 1 over the string value, bypassing the guard. Fixed with 3-line check at top of state_increment. T42 in the test scenarios seed is the permanent regression test for this.

```python
#!/usr/bin/env python3
"""
Agentic OS - Kernel v3 (dual-loop focused)
Seven commands: acquire_lock, release_lock, emit_event, read_events,
state_update, state_increment, claim_task (partition lock only, no registry).

Deliberately minimal. Solves the dual-loop use case: one ORCHESTRATOR,
one INNER_AGENT (or N agents claiming partitions), one laptop.
"""
import os, sys, json, time, uuid, random, argparse
from pathlib import Path
from datetime import datetime, timezone

KERNEL_DIR  = Path(os.environ.get("CLAUDE_PROJECT_DIR", os.getcwd())) / "context"
EVENTS_FILE = KERNEL_DIR / "events.jsonl"
LOCKS_DIR   = KERNEL_DIR / ".locks"
STATE_FILE  = KERNEL_DIR / "os-state.json"
AGENTS_FILE = KERNEL_DIR / "agents.json"
AGENTS_DIR  = KERNEL_DIR / "agents"
EVENTS_MAX_BYTES = 10 * 1024 * 1024  # 10 MB before rotation

# Keys that cannot be overwritten via state_update or state_increment (prompt injection defense)
PROTECTED_STATE_KEYS = frozenset({"execution_mode", "hook_sample_rate"})


def _now():
    return datetime.now(timezone.utc).isoformat().replace("+00:00", "Z")


def _load(path, default):
    try:
        if Path(path).exists():
            with open(path, "r", encoding="utf-8") as f:
                return json.load(f)
    except Exception:
        pass
    return default


def _pid_alive(pid):
    try:
        os.kill(int(pid), 0)
        return True
    except ProcessLookupError:
        return False
    except PermissionError:
        return True


def _is_stale(lock_path):
    meta = _load(lock_path / "meta.json", {})
    if meta:
        if meta.get("pid") and not _pid_alive(meta["pid"]):
            return True
        if meta.get("expires_at", 0) < time.time():
            return True
        return False
    try:
        timeout = _load(STATE_FILE, {}).get("lock_timeout_seconds", 1800)
        return time.time() - lock_path.stat().st_mtime > timeout
    except OSError:
        return True


def _clear(lock_path):
    try:
        for f in Path(lock_path).iterdir():
            f.unlink()
        os.rmdir(lock_path)
    except OSError:
        pass


def _spinlock(lock_path, timeout=30):
    os.makedirs(LOCKS_DIR, exist_ok=True)
    deadline = time.time() + timeout
    while time.time() < deadline:
        try:
            os.mkdir(lock_path)
            return True
        except FileExistsError:
            try:
                if time.time() - Path(lock_path).stat().st_mtime > 20:
                    _clear(lock_path)
            except OSError:
                pass
            time.sleep(random.uniform(0.05, 0.15))
    return False


def _validate_agent(name):
    r = _load(AGENTS_FILE, {})
    if name in r.get("permitted_agents", []):
        return True
    print(f"[Kernel] Unregistered agent: {name}", file=sys.stderr)
    return False


def acquire_lock(name, ttl=None):
    lock = LOCKS_DIR / f"{name}.lock"
    os.makedirs(LOCKS_DIR, exist_ok=True)
    if lock.exists():
        if _is_stale(lock):
            _clear(lock)
            print(f"[Kernel] Stale lock cleared: {name}")
        else:
            print(f"[Kernel] Lock busy: {name}", file=sys.stderr)
            sys.exit(1)
    try:
        os.mkdir(lock)
    except FileExistsError:
        print(f"[Kernel] Race on lock: {name}", file=sys.stderr)
        sys.exit(1)
    effective_ttl = ttl or int(_load(STATE_FILE, {}).get("lock_timeout_seconds", 1800))
    try:
        meta = {"pid": os.getpid(), "acquired_at": _now(),
                "expires_at": time.time() + effective_ttl, "ttl": effective_ttl}
        (lock / "meta.json").write_text(json.dumps(meta), encoding="utf-8")
    except OSError:
        pass
    print(f"[Kernel] Lock acquired: {name} (ttl={effective_ttl}s)")


def release_lock(name):
    _clear(LOCKS_DIR / f"{name}.lock")
    print(f"[Kernel] Lock released: {name}")


def emit_event(agent, type_, action, status=None, summary=None,
               to=None, correlation_id=None):
    if not _validate_agent(agent):
        sys.exit(1)
    if EVENTS_FILE.exists() and EVENTS_FILE.stat().st_size > EVENTS_MAX_BYTES:
        archive = KERNEL_DIR / "events-archive"
        os.makedirs(archive, exist_ok=True)
        rotate_lock = LOCKS_DIR / "events_rotate.lock"
        try:
            os.mkdir(rotate_lock)
            try:
                ts = datetime.now().strftime("%Y%m%d_%H%M%S")
                os.rename(EVENTS_FILE, archive / f"events-{ts}.jsonl")
                print(f"[Kernel] Rotated events.jsonl")
            finally:
                _clear(rotate_lock)
        except FileExistsError:
            pass
    event = {"id": str(uuid.uuid4()), "time": _now(),
             "agent": agent, "type": type_, "action": action}
    if to:             event["to"]             = to
    if correlation_id: event["correlation_id"] = correlation_id
    if status:         event["status"]         = status
    if summary:        event["summary"]        = summary
    write_lock = LOCKS_DIR / "events_write.lock"
    if not _spinlock(write_lock):
        print("[Kernel] Events write lock timeout", file=sys.stderr)
        sys.exit(1)
    try:
        os.makedirs(KERNEL_DIR, exist_ok=True)
        with open(EVENTS_FILE, "a", encoding="utf-8") as f:
            f.write(json.dumps(event) + "\n")
    finally:
        _clear(write_lock)
    print(f"[Kernel] Event emitted: {type_}:{action}" + (f" -> {to}" if to else ""))


def read_events(agent, since=None):
    cursor = since if since is not None else _read_cursor(agent)
    events, lines = [], []
    if EVENTS_FILE.exists():
        lines = EVENTS_FILE.read_text(encoding="utf-8").splitlines()
    if cursor > len(lines):
        cursor = 0
    for line in lines[cursor:]:
        if not line.strip():
            continue
        try:
            ev = json.loads(line)
            if ev.get("to") in (None, agent):
                events.append(ev)
        except json.JSONDecodeError:
            pass
    _write_cursor(agent, len(lines))
    print(json.dumps(events))


def _read_cursor(agent):
    path = AGENTS_DIR / f"{agent}.cursor"
    try:
        return int(path.read_text(encoding="utf-8").strip())
    except Exception:
        return 0


def _write_cursor(agent, n):
    os.makedirs(AGENTS_DIR, exist_ok=True)
    (AGENTS_DIR / f"{agent}.cursor").write_text(str(n), encoding="utf-8")


def state_update(key, value):
    if key in PROTECTED_STATE_KEYS:
        print(f"[Kernel] Protected key: {key}", file=sys.stderr)
        sys.exit(1)
    lock = LOCKS_DIR / "state_write.lock"
    if not _spinlock(lock):
        print("[Kernel] State lock timeout", file=sys.stderr)
        sys.exit(1)
    try:
        s = _load(STATE_FILE, {})
        try:
            s[key] = json.loads(value)
        except (json.JSONDecodeError, TypeError):
            s[key] = value
        os.makedirs(KERNEL_DIR, exist_ok=True)
        with open(STATE_FILE, "w", encoding="utf-8") as f:
            json.dump(s, f, indent=2)
        print(f"[Kernel] State updated: {key}")
    finally:
        _clear(lock)


def state_increment(key):
    # ROUND 8 BUG FIX: state_increment was missing this check that state_update had.
    # An agent calling `state_increment --key execution_mode` would write integer 1
    # over the string value, bypassing the PROTECTED_STATE_KEYS guard entirely.
    if key in PROTECTED_STATE_KEYS:
        print(f"[Kernel] Protected key: {key}", file=sys.stderr)
        sys.exit(1)
    lock = LOCKS_DIR / "state_write.lock"
    if not _spinlock(lock):
        print("[Kernel] State lock timeout", file=sys.stderr)
        sys.exit(1)
    try:
        s = _load(STATE_FILE, {})
        s[key] = int(s.get(key, 0)) + 1
        os.makedirs(KERNEL_DIR, exist_ok=True)
        with open(STATE_FILE, "w", encoding="utf-8") as f:
            json.dump(s, f, indent=2)
        print(s[key])
    finally:
        _clear(lock)


def claim_task(task_id, partition, agent, ttl=300):
    if not _validate_agent(agent):
        sys.exit(1)
    lock = LOCKS_DIR / f"task_{task_id}_p{partition}.lock"
    os.makedirs(LOCKS_DIR, exist_ok=True)
    if lock.exists() and _is_stale(lock):
        _clear(lock)
    try:
        os.mkdir(lock)
    except FileExistsError:
        print("already_claimed")
        return
    try:
        meta = {"pid": os.getpid(), "agent": agent, "expires_at": time.time() + ttl}
        (lock / "meta.json").write_text(json.dumps(meta), encoding="utf-8")
    except OSError:
        pass
    print("claimed")


def main():
    p = argparse.ArgumentParser(description="Agentic OS Kernel v3")
    s = p.add_subparsers(dest="cmd", required=True)

    a = s.add_parser("acquire_lock")
    a.add_argument("name")
    a.add_argument("--ttl", type=int, default=None)

    s.add_parser("release_lock").add_argument("name")

    e = s.add_parser("emit_event")
    e.add_argument("--agent", required=True)
    e.add_argument("--type", required=True)
    e.add_argument("--action", required=True)
    e.add_argument("--status", choices=["success", "fail"])
    e.add_argument("--summary")
    e.add_argument("--to")
    e.add_argument("--correlation-id")

    r = s.add_parser("read_events")
    r.add_argument("--agent", required=True)
    r.add_argument("--since-cursor", type=int, default=None)

    u = s.add_parser("state_update")
    u.add_argument("key")
    u.add_argument("value")

    s.add_parser("state_increment").add_argument("--key", required=True)

    c = s.add_parser("claim_task")
    c.add_argument("--task-id", required=True)
    c.add_argument("--partition", type=int, required=True)
    c.add_argument("--agent", required=True)
    c.add_argument("--ttl", type=int, default=300)

    args = p.parse_args()
    if args.cmd == "acquire_lock":       acquire_lock(args.name, args.ttl)
    elif args.cmd == "release_lock":     release_lock(args.name)
    elif args.cmd == "emit_event":       emit_event(args.agent, args.type, args.action,
                                                    args.status, args.summary,
                                                    args.to, args.correlation_id)
    elif args.cmd == "read_events":      read_events(args.agent, args.since_cursor)
    elif args.cmd == "state_update":     state_update(args.key, args.value)
    elif args.cmd == "state_increment":  state_increment(args.key)
    elif args.cmd == "claim_task":       claim_task(args.task_id, args.partition,
                                                    args.agent, args.ttl)


if __name__ == "__main__":
    main()
```

---

## What is NOT in v36 (Confirmed Backlog)

- All results.tsv baselines empty - no eval scores exist yet (first run establishes baseline)
- update_memory.py still impersonates "system" agent - should emit as "legacy-hook" (P1)
- Improvement ledger initialization not wired into agentic-os-init Phase 3 - must be created manually on first loop run (P2)
- loop-progress-report not registered in plugin.json yet (P2)
- North star regression trigger detection logic is in SKILL description but not enforced by any script or hook (P2)
- Cross-plugin combined progress chart (agentic-os + exploration-cycle in one view) - run separately (P3)
- eval_runner.py keyword stuffing incentive - learning loop can still game its own eval metric (P1, carried from v32)

---

## Review Questions for v36

1. Ledger write atomicity: The improvement ledger is appended by ORCHESTRATOR at Stage 4.7. If two agents run concurrent loops against the same ledger, row interleaving could corrupt the table structure. Should the ledger write be gated behind a context lock? Or is the ORCHESTRATOR-only write pattern sufficient?

2. Survey-to-action trace quality: Section 2 requires the agent to quote the exact friction item and link it to a specific file change and eval delta. This is qualitative data produced by an LLM. What prevents an agent from writing plausible-sounding but hallucinated trace entries?

3. Baseline establishment problem: All results.tsv files are empty. The first eval_runner.py run will always be KEEP (no baseline to beat). Should cycle 1 always be labeled BASELINE, not KEEP?

4. 50 test scenarios as oracle: The test-scenarios-seed.md was designed by the same system that will evaluate them - they could all be easy tests that confirm what already works. Which of the 50 are most likely to reveal real problems?

5. North star metric gaming: human_interventions is counted from events.jsonl. An agent can trivially game this by not emitting friction events. Is there an independent signal that cannot be gamed?

6. Improvement ledger as attractor: ORCHESTRATOR reads Section 1 at every orientation and naturally focuses on skills with flat or declining scores. Should high-performing skills also be regularly tested to confirm improvements are stable?

7. Cross-plugin score normalization: Each plugin has a different results.tsv scoring rubric. Should Section 1 scores be normalized (0-1) before writing, so cross-plugin comparisons are valid?

8. Loop report vs ledger duplication: Both loop report (Stage 4.5) and ledger (Stage 4.7) record eval scores per cycle. Should the loop report be authoritative and the ledger just aggregate from it?
