# Session Log: 2026-04-04 — Eval Backport: convert-mermaid

## What Was Done
- Optimized `convert-mermaid` (plugin: `mermaid-to-png`) from score **0.61 → 1.00** over ~22 iterations (2 rounds)
- Backported:
  - `SKILL.md` — refined description (removes 'convert', 'image', 'mermaid' as standalone FP triggers; adds 'rasterization', 'raster', 'rasterize', 'render', 'export')
  - `SKILL.md` — added `examples:` frontmatter field
  - `SKILL.md` — added `<example>` XML blocks to body (fixed heuristic 0.70 → 1.00)
  - `evals/results.tsv` — appended 20 new score rows documenting improvement trajectory
- Rejected: nothing — all 5 changes were clean ACCEPTs

## Phase 5 Interrogation Findings
- **Proposer prompt did not evolve** — same template throughout both rounds; convergence was achieved through mutation strategy shift (adding keywords → surgically removing bridge words)
- **Hardest problem**: The word "mermaid" itself. Tempting to include (it IS a mermaid skill) but it's the biggest false-positive trigger (e.g. "generate a mermaid diagram"). Solution: remove it from description entirely and rely on distinctive action words.
- **Copilot re-introduces bridge words when asked to "refine"** — future proposer prompts may benefit from an explicit negative constraint list at call time
- **Master template already matches lab proposer prompt** — no template backport needed

## Snags Encountered
- `temp/retrospectives/` was not on the working tree (only in git history) — had to use `git show` to read surveys
- Lab had TWO runs: round 1 (in-memory iterations) + round 2 (3 committed KEEPs). Final committed 1.00 state was `4ba908d`; the `e7be63b` cleanup commit changed the description but was not re-evaluated — used `4ba908d` description for backport (verified 1.00)

## Key Decisions
- Used `4ba908d` description (last evaluated at 1.00) rather than the cosmetically cleaner `e7be63b` version which dropped "rasterization" terms that were proven contributors to accuracy

## Open Items
- [ ] Consider adding a "copilot re-introduces bridge words on refine" guard to `copilot_proposer_prompt.md.template` Trap Warning section
- [ ] evals.json coverage is at 14 cases — could add adversarial prompts for "render PNG from code" vs "render mermaid to PNG" distinction
