# Fallback Tree for optimize-agent-instructions

1. Audit + rewrite instruction files per Karpathy principles -> 2. If a platform-specific section's canonical rules are unclear, consult optimize-agent-instructions/SKILL.md itself rather than guessing from memory.
