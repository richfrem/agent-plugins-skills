# Example gemini probe output captured during a session

During an interactive probe on macOS, the following output was observed when running `gemini --version`:

```
0.38.2
```

This file is a short example intended for downstream skills to see a real-world probe snippet. Replace or append real probe outputs here when running the shell probe script.
