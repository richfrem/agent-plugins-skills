# Evolution Log — Agent Agentic OS

| Date | Tier | Friction / Failure | Patch | Edit Type | Outcome |
|------|------|-------------------|-------|-----------|---------|
| 2026-09-11 | Tier 1 | The bounded implementation loop existed as a low-level utility and test-cadence rules were advisory YAML, allowing callers to bypass the canonical runtime path or repeat expensive final-suite runs. | Added `ControlPlane.create_implementation_controller()` as the canonical persistent-runtime factory, `ImplementationController.run_verification()` with focused→integration→single-final cadence enforcement, and registry guidance naming both APIs. | Fix Forward | GREEN — focused implementation, guidance, and policy tests: 39 passed. |
| 2026-09-10 | Tier 1 | Authorized work packages had no persistent bounded queue or watchdog-backed continuation owner after worktree entry. | Added `PersistentTaskQueue` and `ImplementationController` around the existing lifecycle-independent loop, persisting task status and heartbeat/progress, continuing revise/fix rounds without prompts, and stopping at blockers or explicit exit verification. | Fix Forward | GREEN — focused implementation-loop tests: 7 passed; cross-process locking remains an explicit limitation. |
| 2026-09-10 | Tier 1 | Authorized APPROVED -> IN_WORKTREE commits had no runtime bridge to persist implementation ownership/kickoff or invoke an implementation controller, leaving kickoff behavior to ad-hoc callers and allowing replay duplication. | Added an optional constructor-injected implementation controller; after the atomic transition, persist an idempotent `implementation_kickoff` receipt keyed to the transition actor, then invoke the controller exactly once and propagate callback failures. | Fix Forward | GREEN — focused runtime bridge regression tests cover entry, non-entry transitions, replay, and visible callback failure. |
| 2026-09-10 | Tier 1 | Any lifecycle state could be marked DONE through an adjacency or persistence path without a distinct human force-close authorization contract. | Added wildcard DONE templates, explicit FORCE_CLOSE human decision persistence, coordinator/control-plane denial boundaries, and simulator coverage while preserving RETROSPECTIVE -> DONE and IN_WORKTREE loops. | Fix Forward | Focused state-machine and simulator tests pass; plugin audits pass. |
| 2026-09-10 | Tier 1 | P01 intake guidance could offer authorized source answers and advise premium confirmation, but neither consent scope nor candidate provenance/confirmation was durable. A later stage or review round could therefore not distinguish a fresh human consent from a source-derived candidate. | Added schema v9 `premium_consents` and `source_assisted_answer_candidates`, exact task/stage/round/model consent checks, human-only source confirmation, and an interview-exit guard that blocks pending candidates. Kept lifecycle state untouched. | Fix Forward | GREEN — RED/GREEN P01 contracts, post-install focused suite (17 tests), full Agentic OS suite (367 passed), plugin audits, and symlink diagnose/restore/diagnose all passed. |
| 2026-09-10 | Tier 1 | Repository-wide pytest collection could fail before executing any tests because two standalone plugin smoke programs shared the top-level module name `smoke_test`; the control-plane exit gate also accepted a focused `test_suite` receipt without proving the full repository suite ran. | Added explicit pytest collection exclusions for the standalone smoke programs, introduced the `pytest_full_suite` verifier and `full_test_suite` policy check, required that check on `VERIFY_EXIT -> RETROSPECTIVE`, updated simulator coverage, and migrated affected control-plane fixtures to the stronger contract. | Fix Forward | GREEN for collection and control-plane suites; full suite now runs and reports 728 passed, 2 skipped, with 6 unrelated pre-existing failures. |
| 2026-09-10 | Tier 1 | The pipeline could reach exit verification with green tests even when an approved implementation task from the plan had never been completed. | Added a machine-readable implementation task ledger contract, fail-closed artifact/evidence validation, YAML `implementation_completeness` enforcement on `VERIFY_EXIT -> RETROSPECTIVE`, simulator coverage, and regression tests. | Fix Forward | GREEN — Agentic OS suite: 358 passed; plugin audits passed; symlink diagnosis reports all links OK. |
| 2026-09-08 | Tier 1 | Selecting `MULTI_AGENT_CODE_REVIEW` exposed a gate drift: `MULTI_AGENT_CODE_REVIEW -> VERIFY_EXIT` had policy support for `code_review_or_skip` but its YAML `deterministic_checks` was empty, allowing the review stage to become an unenforced sink. | Added `code_review_or_skip` to the exact YAML edge, documented the PASS-or-explicit-skip requirement, and added a regression test before the fix. | Fix Forward | RED reproduced the missing declaration; GREEN after the targeted YAML correction. |
| 2026-09-08 | Tier 1 | Final documentation could drift from the implemented delegation, advisory-guidance, review-dispatch, and retrospective state-machine contracts even while unit tests remained green. | Added architecture/pipeline Mermaid contract coverage, documented delegation and advisory boundaries in the architecture diagram, and documented transition-guidance plus required-source/canonical-verdict usage in the runtime skills. | Documentation contract | RESOLVED — full pipeline edge parity and 13 diagram/skill contract tests pass. |
| 2026-09-08 | Tier 1 | Critic review persistence normalized external `REQUEST_CHANGES` to `REVISE`, but the CLI acknowledgement still exposed the external wording and lacked direct regression coverage for the CLI/API boundary and PASS-only approval semantics. | Added focused control-plane tests for real CLI dispatch, canonical persistence/reporting, undeclared verdict rejection, and PASS-only gate satisfaction; updated the CLI acknowledgement to report canonical `REVISE`. | Fix Forward | RESOLVED — full affected suites passed with canonical verdict behavior preserved. |
| 2026-09-08 | Tier 1 | Transition guidance was free-form YAML text rendered after a transition, so agents had to re-derive legal next states and could mistake stale guidance for authority. The control plane needed actionable guidance before and after transitions without creating a second state-machine authority. | Added a versioned, read-only registry guidance snapshot. Legal next states are derived from `state_machine.ALLOWED_TRANSITIONS`; YAML contributes only optional success wording and helper commands. Added fail-closed denial guidance for illegal requested edges, coordinator before/after rendering, a `transition-guidance` CLI/API, and regression tests proving stale YAML claims cannot authorize an illegal edge. | Fix Forward | RESOLVED — focused guidance/registry suite 12/12 passing; advisory output explicitly preserves policy, human-gate, and SQLite authority. |
| 2026-09-07 | Tier 3 | GitHub issue #523: `agent_control.py`'s `policy.py`/`TransitionRegistry` enforcement (successor to the original `GATE_REQUIREMENTS` registry) only lived in the Python application layer — any actor with filesystem access could bypass every gate with a single raw `sqlite3 context/control_plane.db "UPDATE tasks SET state='DONE'..."`. During TDD implementation of the fix, two further real bugs were found empirically (not by the prior single-agent security review or the `/tmp` prototype, which only ever exercised one trigger in isolation): (1) the INSERT trigger's original coerce-to-`INTAKE` corrective action fired the separate UPDATE trigger as a side effect, which then reverted the coercion right back (e.g. `DONE -> INTAKE` isn't a legal edge), silently defeating the fix; (2) `_rebuild_schema_transactional()`'s `ALTER TABLE RENAME` step re-points existing triggers to follow the rename rather than dropping them, so `enforce_valid_transition` survived bound to the wrong table and was then permanently destroyed when the migrating table was dropped — meaning every schema rebuild would have silently disabled all UPDATE-side enforcement. | Added `valid_transitions` (synced from `TransitionRegistry.get_all_edges()` on every `ensure_schema()` call — self-maintaining, no manual step per DAG change) and `transition_violations` audit tables, plus two triggers: `AFTER UPDATE` reverts `state`+`updated_at` to `OLD.*` on illegal transitions (never destroys — an existing task's history is worth protecting); `AFTER INSERT` **deletes** illegally-seeded initial-state rows instead of coercing (a fresh insert has no history to protect, and delete avoids the UPDATE-trigger cascade). Both triggers now explicitly `DROP`+recreated around `_rebuild_schema_transactional()`, not left to `CREATE TRIGGER IF NOT EXISTS` alone. Also verified and accepted as a documented residual risk (not fixed): `PRAGMA recursive_triggers = ON` causes the UPDATE trigger's correction to oscillate until SQLite's recursion-depth cap aborts the statement — the illegal write never persists, but the violation goes unlogged and the caller sees an unhandled `sqlite3.OperationalError`; not SQLite's default setting, so accepted rather than designing a lock/sentinel mechanism. Scoped as a deliberate partial fix throughout (adjacency-legality only, not full `policy.py` parity) — see `docs/plans/issue-523-trigger-enforcement-spec.md` for the full guardrail table and all disclosed residual risks. | Fix Forward | RESOLVED — 8/8 new trigger-enforcement tests passing (2 of them regression tests for the two implementation-time bugs above), full `-k control_plane` suite green apart from 8 confirmed-pre-existing, unrelated failures in `test_control_plane_policy.py`/`test_control_plane_facade_composition.py` (byte-identical reproduction against unmodified main). Structural audit clean; `audit.py`'s 45 findings confirmed byte-identical against unmodified main (zero introduced by this change) — filed separately as its own batch-fix candidate rather than bundled in. Reviewed via single-agent `cli-agents:security-auditor` dispatch (REVISE verdict, all 7 findings disposed) before implementation began. |
| 2026-09-07 | Tier 2 | Post-commit code review (single-agent `cli-agents:security-auditor` dispatch, verdict "Proceed with changes") of #523's committed implementation (`c9019f5b`) found `_sync_valid_transitions()`'s `DELETE`+`INSERT` had no transaction wrapper — on the multi-worktree shared DB, a failure or concurrent reader between the two statements could observe `valid_transitions` as empty/partial, during which every legitimate transition would be misclassified as illegal and reverted/deleted. A second finding — `_rebuild_schema_transactional()`'s `BEGIN IMMEDIATE`/`COMMIT` wrapper doesn't actually protect against partial migration on exception, since `conn.executescript()` silently commits mid-transaction — was correctly identified as pre-existing (predates #523, dates to issue-524's Step 5) rather than caused by this branch's own new code. | Wrapped `_sync_valid_transitions()`'s `DELETE`+`INSERT` in an explicit `BEGIN IMMEDIATE`/`COMMIT`/`ROLLBACK`. TDD: wrote a failing regression test first (`test_sync_valid_transitions_is_atomic_not_left_empty_on_failure`) that injects a real NOT-NULL constraint violation mid-batch via a patched `TransitionRegistry`, confirmed RED (17 of 53 rows survived a bare-statement failure — proving the partial-write bug was real, not theoretical), then applied the fix and confirmed GREEN (all 53 rows retained after the same forced failure). The pre-existing `_rebuild_schema_transactional()` finding was filed separately as GitHub Issue #552, not fixed in this branch — same "fix what this branch's own new code caused, file what predates it" discipline applied to the earlier `audit.py` findings (#551). | Fix Forward | RESOLVED — 9/9 trigger-enforcement tests passing (1 new), full `-k control_plane` suite still green apart from the same 8 confirmed-pre-existing failures. |
|------|------|-------------------|-------|-----------|---------|
| 2026-09-07 | Tier 0 | Both review-gate transition templates (`draft_plan_to_multi_agent_review`, `worktree_review_to_multi_agent_code_review`) only asked how many reviewers (skip/single/multi), silently deciding *how* the review would be conducted (internal sub-agent dispatch) rather than exposing it as a user choice — no way to request an external bundle review (context-bundler output handed to the user for an independent model) instead of an internal sub-agent dispatch. | Added a second explicit axis (internal vs. external bundle) to both templates' `human_questions`, expressed as one flat 5-option question (skip + single-internal + single-external + multi-internal + multi-external) rather than two nested questions — the coordinator's `human_questions` list is unconditional (every question always asked, no conditional branching support), so a two-question design would force an irrelevant internal-vs-external answer even when skip was chosen. Scoped to exactly these two edges; no other review-gate templates touched. | Fix Forward | RESOLVED — verified end-to-end via a disposable throwaway task driven through both edges, confirming the 5-way question renders and each answer commits correctly. Unrelated to and developed independently of issue #523 (trigger enforcement); split into its own branch (`fix/review-gate-method-choice`) rather than bundled into that PR. |
| 2026-09-06 | Tier 0 | issue-524 external review round 4: "VERIFIED WITH MINOR CORRECTIONS" — all architectural blockers from rounds 1-3 confirmed genuinely fixed; no further redesign or review round required. Three textual/test-naming corrections requested: (1) `agent_control.py`'s module docstring still described state-machine adjacency checks and tier-selection strategy as ControlPlane's own remaining code, stale since round 3 moved both out; (2) the map-debt invariant entry said "behind dedicated ports/adapters" for all 7 responsibilities, inaccurate since state-machine validation and gate policy are pure domain classes with no port/adapter pair; (3) a model-catalog test named/documented as "end_to_end" actually only tested the adapter's private helper methods individually, never calling the public `resolve_recommended_model()`. | Corrected the docstring to describe ControlPlane as pure application orchestration coordinating the composed components, explicitly noting the state-machine/tier-selection relocation. Corrected the map-debt wording to "separated into dedicated, independently testable domain components or port/adapter boundaries." Renamed the test to `test_model_catalog_adapter_helper_components_against_temp_files` with an honest docstring, and added a new `test_model_catalog_adapter_resolve_recommended_model_against_real_repo_files` that genuinely calls the public method end-to-end against this repo's real `plugins/cli-agents/references/` files. | Fix Forward | 173/173 tests passing (1 new). Structural audit clean. Four review rounds complete; cleared to proceed toward PR. |
| 2026-09-06 | Tier 2/3 | issue-524 external review round 3 (ChatGPT 5.6): "CLAIMS NOT FULLY SUPPORTED / REQUEST_CHANGES" on the round-2 correction bundle. Confirmed round-1/2 fixes held (persistence, clock, fail-closed policy, wording, symlinks, map-debt ID). Three new blocking findings: (1) `CANONICAL_STATES`/`ALLOWED_TRANSITIONS`/adjacency validation still lived directly in `ControlPlane.transition()` — the diagram's "State-machine validation" node had no corresponding component in code at all; (2) `ModelCatalogAdapter` only covered JSON file reads — `_resolve_tool_catalog`, `_pick_tier_model`, and all tier-selection/fallback logic remained in `ControlPlane`, contradicting the plan's "move resolve_recommended_model and helpers" wording; (3) `PersistencePort` was not constructor-injectable — the facade test worked around this by monkeypatching `cp._persistence` after construction, constructing a real `SqlitePersistenceAdapter` first and discarding it. Plus a behavioral regression risk: `read_current_state()`/`apply_transition()`/the `count_*`/`has_*` methods didn't call `ensure_schema()`, so `update_worktree()` against a fresh DB could raise `sqlite3.OperationalError: no such table: tasks` instead of the original auto-init behavior. | (1) Added `control_plane/state_machine.py`'s `StateMachine` (pure domain class, no port/adapter needed — no infrastructure to abstract): `validate_known_state()`/`validate_adjacency()`, moved verbatim from `ControlPlane.transition()`; `CANONICAL_STATES`/`ALLOWED_TRANSITIONS`/`InvalidStateTransition` moved there too, re-imported into `agent_control.py` for backward compat. (2) Added `ModelCatalogPort.resolve_recommended_model()` (new abstract method); `ModelCatalogAdapter` now implements the full resolution (tool-alias + tier-strategy + fallback, moved verbatim); `ControlPlane.resolve_recommended_model()` is now a one-line delegate. (3) Added `persistence_adapter` as a true constructor parameter (`ControlPlane(persistence_adapter=fake)`); rewrote the facade-composition delegation test to use it instead of monkeypatching. (4) Added `self.ensure_schema()` to all 6 `SqlitePersistenceAdapter` methods that lacked it, plus a regression test proving `update_worktree()` against a brand-new DB fails with the expected domain error ("Task not found"), never a raw SQLite schema error. Updated the diagram's State-machine node to name the real `StateMachine` class, and strengthened the diagram-contract test: verifies the state-machine class actually exists, the policy→persistence dotted edge targets `PersistencePort` specifically, every adapter actually `issubclass()`-subclasses its corresponding port (not just similar naming), and basic Mermaid-renderability (balanced brackets, no smart quotes, no raw HTML). Added symlinks for the new `state_machine.py` into `interview-spec`. | Fix Forward | 172/172 tests passing (up from 161 — 11 new: StateMachine unit/integration tests, ModelCatalogAdapter end-to-end test, fresh-DB regression test, 4 new diagram-contract tests). Re-verified CLI `--help` output and `_map_exception_to_exit_code` byte-identical to baseline after this third rewrite pass. Re-verified via grep: zero SQL/state-machine-logic/model-tier-logic remaining in `agent_control.py`. |
| 2026-09-06 | Tier 1 | issue-524 lacked a durable architecture diagram for the hexagonal decomposition — future work on related issues (#519, #523, #529) needs to understand where policy/persistence/authorization boundaries live, and prose-only documentation drifts silently from code. | Added `docs/diagrams/control-plane-architecture.mermaid` (flowchart: Callers -> ControlPlane facade -> {state-machine, policy engine, 5 ports} -> 5 concrete adapters -> infrastructure boundaries), generated from and verified against the final corrected implementation (not the pre-correction state). Added a permanent architecture-contract test (`test_control_plane_architecture_diagram_contract.py`) that parses `ports.py`/`adapters.py` via `ast` and fails if the diagram doesn't name every current port/adapter class, plus a duplicate SQL-absence check independent of the Step 8 facade-composition test (so the two can fail with distinct, specific messages). Linked the diagram from `README.md` next to the existing pipeline diagram. | Fix Forward | 161/161 tests passing (5 new diagram-contract tests). Structural audit clean. |
| 2026-09-06 | Tier 2/3 | issue-524 post-implementation external review (round 2, ChatGPT 5.6) verdict: REQUEST_CHANGES, correctly disproving the "all 7 responsibilities separated / ControlPlane is a thin facade" claim from Step 9. Confirmed findings: (1) `SqlitePersistenceAdapter` declared `PersistencePort` in Step 2 but never implemented/subclassed it — Step 5 only covered connection+migration, leaving all task/transition/receipt CRUD SQL still directly embedded in `ControlPlane` (`_build_transition_policy_ctx`'s inline SQL closures were the clearest example, including a stale comment saying persistence was "not yet extracted (Step 5)" after Step 5 was marked complete); (2) `ClockPort` was declared but dead — `record_verification_receipt()` still called `time.time()` directly, migration logging still called `time.strftime()`; (3) Step 8's composition tests only checked naive substrings (`"sqlite3.connect("`, `"hashlib"` absent from source) — the hashlib check even false-failed on its own docstring's prose — proving nothing about actual delegation; (4) `policy.py`'s `_run_rule()` failed OPEN (`else: satisfied = True`) for an unrecognized rule `check` type; (5) overstated claims: "constructor signature unchanged" (it gained 3 new optional params), "symlink diagnostics clean" (36 pre-existing unrelated broken links exist; only "zero *new*" was actually true); (6) a mislabeled characterization test (docstring said "no prior-art scan logged" when the test actually logs it before a different edge); (7) a duplicate map-debt ID (`DEBT-20260906-09` used twice). | Completed the persistence boundary for real: `SqlitePersistenceAdapter` now subclasses `PersistencePort` and implements all 16 of its methods (`get_task`, `insert_task`, `read_current_state`, `apply_transition`, `count_*`, `has_*`, `insert_*`, `get_verification_receipts`, `update_worktree_fields`, `ensure_schema`) — moved verbatim from `ControlPlane`. `ControlPlane`'s CRUD methods (`create_task`, `get_task`, `transition`, `lock_verifiers`, `verify_sovereignty`, `record_critic_review`, `record_verification_receipt`, `get_verification_receipts`, `update_worktree`, `log_asymmetric_persistence`) now purely delegate — zero `SELECT`/`INSERT`/`UPDATE`/`conn.execute`/`sqlite3` anywhere in `agent_control.py` (verified by grep, not just claimed). Added `ClockPort.strftime()` + `ClockAdapter`, wired into both `record_verification_receipt()` and `SqlitePersistenceAdapter`'s map-debt logging — zero direct `time.time()`/`time.strftime()` calls remain outside `ClockAdapter` itself. Rewrote Step 8's tests to use AST-based import checks (not substring search) plus a full fake-`PersistencePort`-injection test proving 6 operations actually delegate (task creation, lookup, transition, receipts, critic review, worktree update) with zero SQLite. Fixed `policy.py` to fail CLOSED (`PolicyConfigurationError`, deliberately not a `PolicyViolation` subclass, so it can't be silently converted) with a regression test. Corrected the constructor-compatibility and symlink-DoD wording to state precise, verifiable claims instead of unqualified ones. Renamed the misleading characterization test and its docstring. Renumbered the duplicate map-debt row to `DEBT-20260906-10`. | Fix Forward | 156/156 tests passing (up from 155 — new fake-persistence-delegation and fail-closed-policy tests). Re-verified CLI `--help` output and `_map_exception_to_exit_code` byte-identical to pre-refactor baseline after this deeper rewrite. Re-verified via direct grep: zero SQL/sqlite3/hashlib/time usage in `agent_control.py` outside prose. |
| 2026-09-06 | Tier 2 | issue-524 Step 9 (DoD verification): `symlinks.json` file-level-symlinks `agent_control.py` into `plugins/agent-agentic-os/skills/interview-spec/scripts/` (per ADR-003), but `agent_control.py` now hard-depends on the `control_plane/` subpackage, which had no symlink entry — the interview-spec skill's installed copy of `agent_control.py` would have failed to import (`ModuleNotFoundError: No module named 'control_plane'`) the moment a real user installed/used this skill. Caught during Step 9's symlink-diagnostics DoD check, not by any test (no existing test exercised the skill's own scripts/ directory). | Added 4 file-level symlinks via `symlink_manager.py create` (never raw `ln -s`, per plugin-architecture-policy.md Section 5): `__init__.py`, `ports.py`, `adapters.py`, `policy.py` all now symlinked from `interview-spec/scripts/control_plane/` to the canonical `plugins/agent-agentic-os/scripts/control_plane/` source. Verified the fix by importing `agent_control` with `sys.path` pointed at the skill's own scripts/ directory (not the plugin root) — confirmed working. `symlink_manager.py diagnose` shows 36 pre-existing broken links (unrelated `ADRs/*.md` in `agent-scaffolders` skills) — left untouched per surgical-changes scope. | Fix Forward | 154/155 tests passing repo-wide (1 pre-existing unrelated skip), unchanged by this fix. Zero new broken symlinks. |
| 2026-09-06 | Tier 2/3 (in progress) | issue-524 Step 8: after Steps 3-7 extracted all 4 infrastructure adapters, needed to confirm `ControlPlane` actually composes them as a thin facade rather than silently re-absorbing their responsibilities back in — the exact "replacement god-service" failure mode the plan's guardrails warned against. | Rewrote `agent_control.py`'s module docstring to explicitly document the composition architecture (`self._fs`/`self._crypto`/`self._model_catalog`/`self._persistence` and what each does/doesn't own). Added a permanent structural test suite confirming: (1) a default-constructed `ControlPlane` wires all 4 real adapter types; (2) the module source contains zero bare `sqlite3.connect(` calls outside the persistence adapter; (3) zero `hashlib` references outside the crypto adapter. | Fix Forward (in progress) | 154/155 tests passing repo-wide (1 pre-existing unrelated skip). New: 3 facade-composition structural tests. |
| 2026-09-06 | Tier 2/3 (in progress) | issue-524 Step 7: `resolve_recommended_model()`'s inline `json.loads()`/`.exists()` calls for reading `plugins/cli-agents/references/*.json` catalog files were direct filesystem/JSON infrastructure inside `ControlPlane` — the last of the 7 god-object responsibilities (model-catalog resolution) still unextracted. | Added `ModelCatalogAdapter` implementing `ModelCatalogPort` (`load_catalog`, `load_cheapest` — interface already declared in Step 2) in `control_plane/adapters.py`. `ControlPlane.__init__` now accepts an optional `model_catalog_adapter` (defaults to `ModelCatalogAdapter()`); `resolve_recommended_model()` routes both file reads through `self._model_catalog`. Tier-selection/strategy logic (`_pick_tier_model`, `_resolve_tool_catalog`) stays in `ControlPlane` as orchestration — out of this step's scope, matching the plan's "move resolve_recommended_model and helpers" wording for the file-I/O piece specifically. | Fix Forward (in progress) | 151/152 tests passing repo-wide (1 pre-existing unrelated skip). New: 7 model-catalog-adapter unit/integration tests. Structural audit clean. All 7 of the issue's originally-identified god-object responsibilities now have a dedicated port/adapter seam (Steps 3-7); remaining work is thinning the ControlPlane facade itself and final DoD verification. |
| 2026-09-06 | Tier 2/3 (in progress) | issue-524 Step 6: the module-level `_sha256_file()` function and inline `hashlib.sha256()` receipt-token hashing in `record_verification_receipt()` were direct infrastructure concretions inside `ControlPlane`/module scope — one of the 7 god-object responsibilities (cryptographic receipts). | Added `CryptoAdapter` implementing `CryptoPort` (`sha256_file`, `sha256_hex`) in `control_plane/adapters.py`. `ControlPlane.__init__` now accepts an optional `crypto_adapter` (defaults to `CryptoAdapter()`); `lock_verifiers()`/`verify_sovereignty()` route through `self._crypto.sha256_file()`, `record_verification_receipt()` routes through `self._crypto.sha256_hex()`. Removed the module-level `_sha256_file()` entirely (confirmed via a permanent AST-based structural test) and the now-orphaned `import hashlib` from `agent_control.py`. | Fix Forward (in progress) | 144/145 tests passing repo-wide (1 pre-existing unrelated skip). New: 6 crypto-adapter unit/integration tests. Structural audit clean. |
| 2026-09-06 | Tier 2/3 (in progress) | issue-524 Step 5: connection management (`_get_connection`, `_discover_shared_db_path`) and the schema migration engine (`init_db`, `_schema_needs_rebuild`, `_rebuild_schema_transactional`, `_copy_common_columns`, `_merge_orphaned_tasks_old`, plus `SCHEMA_SQL`/`SCHEMA_MIGRATIONS`/`CURRENT_SCHEMA_VERSION`/`CHILD_TABLES`/`ALL_REBUILD_TABLES`) were mixed directly into `ControlPlane`, one of the 7 god-object responsibilities. A pre-existing test (`test_control_plane_uses_injected_filesystem_port_for_orphan_merge_log`, added Step 3) called `_log_orphan_merge_conflicts` directly on `ControlPlane` and broke once that method legitimately moved to the persistence adapter. | Added `SqlitePersistenceAdapter` in `control_plane/adapters.py` — verbatim behavior move of all connection/migration logic, including `_log_orphan_merge_conflicts` (composes `FilesystemPort`, preserving the Step 3 seam). `ControlPlane.__init__` now composes `self._persistence = SqlitePersistenceAdapter(...)`; `_get_connection()`/`init_db()` became one-line delegations; `self.db_path` kept as a convenience mirror for introspection. Fixed the Step 3 test to call `cp._persistence._log_orphan_merge_conflicts(...)` instead — confirmed as legitimate relocation, not new dead code. Backward-compat: `CURRENT_SCHEMA_VERSION` re-exported from `agent_control` for existing `from agent_control import CURRENT_SCHEMA_VERSION` callers. Removed the now-orphaned `import subprocess` from `agent_control.py` (moved with the discovery logic). | Fix Forward (in progress) | 138/139 tests passing repo-wide (1 pre-existing unrelated skip). New: 5 adapter-level unit tests. Structural audit clean. |
| 2026-09-06 | Tier 2/3 (in progress) | issue-524 Step 4 (the plan's highest-risk step): gate policy was scattered across 4 independent mechanisms — the `GATE_REQUIREMENTS` registry, 3 hardcoded `_check_*_guard` methods, and `update_worktree()`'s own `pushed_to_origin` barrier — with the push barrier structurally unable to fit the transition-keyed registry (it's an operation guard, not a `(from_state, to_state)` edge), per external plan review round 1. | Added `control_plane/policy.py`: one declarative engine evaluating two rule kinds — `TRANSITION_RULES`/`TO_STATE_RULES` (lifecycle transitions, formerly `GATE_REQUIREMENTS` + the 3 guards) and `OPERATION_RULES` (controlled operations, formerly the push barrier) — both raising the same `PolicyViolation`, re-raised as `PersistenceInvariantViolation` in `agent_control.py` to preserve the exact CLI exit-code contract. Removed all 4 old mechanisms and their helper methods entirely (`_check_prior_art_guard`, `_check_done_guard`, `_check_rolled_back_guard`, `_check_gate_requirement`, `_gate_receipt_exists`, `_gate_critic_review_pass_exists`, `_gate_any_of`, `GATE_REQUIREMENTS`) — confirmed via a permanent AST-based structural test, not just a manual grep. | Fix Forward (in progress) | 133/134 tests passing repo-wide (1 pre-existing unrelated skip) — the Step 1 characterization matrix (38 tests) stayed green throughout, proving behavior preservation across the riskiest step. New: 11 policy-engine unit tests running in 0.03s with zero SQLite fixtures — the exact testability benefit the issue asked for. |
| 2026-09-06 | Tier 2/3 (in progress) | issue-524 Step 3: `_log_orphan_merge_conflicts()` wrote directly to `references/map-debt.md` via a raw `open()`, and (discovered while wiring the seam) `test_control_plane_worktree_sharing.py`'s fixture copied only `agent_control.py` into its throwaway repo — a pattern that would have silently broken once `agent_control.py` gained a real dependency on the new `control_plane/` subpackage. | Added `FilesystemAdapter` in `control_plane/adapters.py` implementing `FilesystemPort`; `ControlPlane.__init__` now accepts an optional `fs_adapter` (defaults to `FilesystemAdapter()`, preserving existing behavior for all current callers) and `_log_orphan_merge_conflicts()` routes through `self._fs` instead of a raw `open()`. Fixed the stale test fixture to `copytree` the `control_plane/` package alongside `agent_control.py` — confirmed this is fixture staleness, not a production risk, since the real plugin installer (`plugin_installer.py`) already deploys whole directories via `copytree`. | Fix Forward (in progress) | 122/123 tests passing repo-wide (1 pre-existing unrelated skip), zero regressions. New: 46 tests in the issue-524 test files (up from 38). |
| 2026-09-06 | Tier 2/3 (in progress) | issue-524 Step 2: no ports/interfaces existed to separate domain/policy logic from concrete infrastructure (sqlite3, subprocess, hashlib, filesystem, model-catalog JSON storage) inside `ControlPlane`. | Added `plugins/agent-agentic-os/scripts/control_plane/` package with `ports.py` declaring `PersistencePort`, `FilesystemPort`, `CryptoPort`, `ClockPort`, `ModelCatalogPort` as ABCs. Added `test_control_plane_ports.py` with a static AST-based check enforcing the dependency-direction invariant (no sqlite3/subprocess/hashlib/time/os imports in the ports module) plus ABC-instantiation and standalone-import checks. Purely additive — nothing wired in yet, no behavior change. | Fix Forward (in progress) | 41/41 tests passing (38 prior + 3 new). |
|------|------|-------------------|-------|-----------|---------|
| 2026-09-06 | Tier 2/3 (in progress) | GitHub issue #524: `agent_control.py`'s `ControlPlane` mixes 7 responsibilities, with gate policy specifically scattered across 4 independent mechanisms (`GATE_REQUIREMENTS` registry, 3 hardcoded `_check_*_guard` methods, and `update_worktree()`'s own `pushed_to_origin` barrier) — a maintainer adding a new gate rule has to know which of 4 places applies, the same drift class that produced prior FK-corruption and DONE-guard bugs. | Step 1 of the approved plan (`docs/plans/issue-524-spec.md`, approved after 2 rounds of external plan review): added `test_agent_control_gate_characterization.py`, an explicit characterization matrix proving every branch of the 4 scattered gate mechanisms — including each of the 3 currently-permitted push states (`WORKTREE_REVIEW`, `MULTI_AGENT_CODE_REVIEW`, `VERIFY_EXIT`) individually, plus the blocked case — as the regression oracle for the upcoming unification (Step 4). No production code changed yet. | Fix Forward (in progress) | Baseline established — 38/38 tests passing (31 existing + 7 new). Remaining steps (ports/contracts, filesystem seam, gate-policy unification, persistence/receipts/model-catalog extraction, facade thinning) tracked in the same worktree/branch, single PR at completion. |
|------|------|-------------------|-------|-----------|---------|
| 2026-09-06 | Tier 2 | agent_control.py's init_db() rename-based migration let SQLite auto-repoint child tables' FKs to a stale _tasks_old, permanently corrupting FK integrity if the DROP didn't complete (hit live in this repo's own control_plane.db, blocking task creation). transition() also had no verification behind phase-gate transitions and a check-then-act race (no WHERE state=? guard on the UPDATE). | Replaced the migration with a versioned, transactional, self-healing rebuild (renames all 6 tables together under one explicit transaction). Added GATE_REQUIREMENTS registry to transition() with skippable-with-recorded-decision reviews (record_review_skip) and non-skippable human_approval/test_suite gates. Fixed the race by reading current_state once and reusing it for both the adjacency check and the guarded UPDATE (ConcurrentModificationError on mismatch). | Fix Forward | RESOLVED — 26/26 tests passing (10 new), structural/compliance audits clean. Related findings (worktree DB isolation, control_plane.db durability, next_number.py worktree resolution, gh_issue_search dedup bug) filed as GitHub Issues #519/#520 and map-debt, deferred to future branches. |
| 2026-09-06 | Tier 1/2 | Follow-up audit findings (TASK-AGENTIC-OS-AUDIT-2): (1) init_agentic_os.py's CONTROL_PLANE_SCHEMA_SQL was a hand-copied duplicate of agent_control.py's schema, missing task_type/schema_version, reproducing the FK-corruption bug on every consumer --retrofit; (2) pre-push-review-guard silently passed (exit 0, zero output) when no control_plane.db or no matching task existed, making an ungated push indistinguishable from a properly-gated one; (3) ControlPlane's db_path auto-discovery walked up for the nearest .git, which resolves separately per git worktree (each worktree got its own disconnected, empty control_plane.db, invisible to task state registered in the main checkout). | (1) _init_control_plane_db() now delegates to agent_control.py's own ControlPlane class instead of duplicating the schema. (2) Both silent-pass branches now print a visible "⚠️ WARNING" to stderr before exiting 0 (still non-blocking, but no longer indistinguishable from a real gate pass). (3) Replaced the manual .git walk-up with `git rev-parse --git-common-dir`, which resolves to the same physical .git directory for every worktree of one repo — all worktrees now share one control_plane.db. | Fix Forward | RESOLVED — 99/100 tests passing (1 pre-existing unrelated skip), 4 new tests (schema parity, 2 silent-pass warnings + 1 regression, cross-worktree sharing), structural/compliance audits clean. |
| 2026-09-06 | Tier 0 | Pipeline diagram and stage-gate wording used raw internal state names (DRAFT_PLAN, MULTI_AGENT_REVIEW) inaccessible to non-technical readers; diagram was also incomplete relative to the real ALLOWED_TRANSITIONS (missing PLAN_REVIEW connectivity, revision loops, escalation paths). | Rewrote docs/diagrams/control-plane-pipeline.mermaid + README copy with plain-language "Step N" labels via mermaid state-label syntax; verified programmatically edge-for-edge against ALLOWED_TRANSITIONS (zero missing, zero extra). Added a second, simplified happy-path diagram for quick reading. Updated interview-spec SKILL.md and pre-push-review-guard user-facing question text to plain language. | Fix Forward | RESOLVED — documentation/wording only, no behavioral change, 100/100 tests passing. |
| 2026-09-06 | Tier 2/3 | Round 1 external multi-agent review (Opus architecture + security personas) of the merged control plane confirmed 4+ real, verified-against-code findings: schema_version written but never read to drive migration decisions (still pure DDL-string-sniffing); verify_sovereignty() never wired into any gate (advisory only); DONE guard accepted ANY exit_code=0 receipt, so an unrelated human_approval receipt satisfied it without any test ever running; ConcurrentModificationError fell into the generic exit(1) handler, indistinguishable from a crash; hardcoded investment_screener/ (a specific consumer repo) paths leaked into the generic scaffolder/guards/CI template deployed into every consumer repo; CI workflow interpolated ${{ github.base_ref }} directly into a run: shell block (script-injection anti-pattern). | Fixed all 6: _schema_needs_rebuild() now checks schema_version.version < CURRENT_SCHEMA_VERSION as the primary trigger; verify_sovereignty() called from _check_done_guard() whenever any verifiers were locked for the task; DONE guard now requires gate_name=test_suite specifically; ConcurrentModificationError mapped to its own exit code (3) via new _map_exception_to_exit_code(); removed investment_screener/ from pre-commit-evolution-guard, turn_evolution_guard.py, init_agentic_os.py CI template, and this repos own .github/workflows/verify-evolution-integrity.yml; moved github.base_ref into an env: block, referenced as "$BASE_REF" in the shell. | Fix Forward | RESOLVED (this batch) — 104/104 tests passing (6 new/updated). Remaining findings from the same review round (SQL injection in pre-push-review-guard via unsanitized branch name, iteration CHECK(1-3) contradicting 1-or-N-rounds design, blanket except Exception in migrations, receipt-authentication reframing for issue #519) tracked separately, in progress same branch. |
| 2026-09-06 | Tier 2/3 | Remaining 4 findings from the same round-1 review: SQL injection in pre-push-review-guard via unsanitized git branch name interpolated into a raw sqlite3 query (PoC: `git checkout -b "x'OR(state)IN('VERIFY_EXIT')--"` fooled the guard into treating an unrelated cleared task as this branch's); blanket except Exception: pass around SCHEMA_MIGRATIONS hid real DB errors behind the "column already exists" assumption; critic_reviews.iteration CHECK(1-3) contradicted the documented "1 or N review rounds" design, would throw IntegrityError on a 4th round. | Branch name now validated against ^[A-Za-z0-9._/-]+$ before use in the SQL query, failing closed (ungated warning) on anything else; except narrowed to sqlite3.OperationalError matching "duplicate column" specifically, anything else propagates; iteration CHECK relaxed to >=1 (schema_version bumped to 3, self-healing rebuild picks it up automatically). Also commented on GitHub Issue #519 reframing scope per both reviewers: the same non-provability defeating discretionary skip-receipts also defeats the supposedly-unskippable human_approval gate itself, and the previously-proposed HMAC-with-local-key fix does not close it (agent can read its own signing key) - needs an external-channel proof mechanism instead. | Fix Forward | RESOLVED - all 10 findings from round-1 review now fixed. 107/107 tests passing (10 new/updated this branch total), structural/compliance audits clean. Ready for round-2 re-review. |
| 2026-09-07 | Tier 2 | GitHub issue #552: `_rebuild_schema_transactional()`'s `BEGIN IMMEDIATE`/`COMMIT` wrapper didn't actually protect against partial migration on exception — `conn.executescript(SCHEMA_SQL)` mid-transaction silently commits any open transaction (documented CPython `sqlite3` behavior) and re-enables `PRAGMA foreign_keys` via `SCHEMA_SQL`'s own leading statement, so everything after that call ran unprotected. Found and diagnosed during #523's implementation, filed separately since the bug predates #523. During this issue's own implementation, live incidents confirmed the bug empirically: `context/control_plane.db` was found already stuck mid-rebuild (7 real task records trapped in `_tasks_migrating` orphan tables, `tasks` empty) from an earlier interrupted rebuild that same session; recovery itself hit two further near-misses (a naive copy silently deleted all 7 rows via `enforce_valid_initial_state` since `valid_transitions` was empty; a second recovery attempt via the real `ensure_schema()` re-triggered the same bug a second time). A third, previously-undocumented symptom was also found via a new non-empty-data unit test: the FK re-enable, combined with SQLite's foreign-key-auto-repoint-on-rename behavior, let `DROP TABLE "_tasks_migrating"` cascade-delete not-yet-copied rows in `_task_transitions_migrating` — silent data loss with no exception raised at all. | Replaced `conn.executescript(SCHEMA_SQL)` with individual `conn.execute()` calls over statements split via a new `_split_schema_sql_statements()` helper (`sqlite3.complete_statement()`-driven parsing, correctly preserving both `CREATE TRIGGER ... BEGIN ... END;` bodies intact despite embedded semicolons), filtering out all 3 leading `PRAGMA` statements so `foreign_keys` stays `OFF` for the method's entire duration — this closes the FK-cascade bug structurally (table-order-independent), not just the one instance found. Also deferred `_log_orphan_merge_conflicts()`'s `map-debt.md` filesystem write until after `COMMIT` succeeds (a plain filesystem append has no SQL rollback, so logging it pre-commit let a rolled-back rebuild leave a permanent, misleading entry for a migration that never completed — found by the spec-stage review), wrapped in its own try/except so a filesystem failure there doesn't masquerade as a rebuild failure to every `ensure_schema()` caller (found by the implementation-stage review). Added `_check_no_orphaned_migration_tables()` as `ensure_schema()`'s literal first operation, failing loudly with an actionable message instead of a cryptic `FOREIGN KEY constraint failed` on a DB already stuck mid-rebuild — verified safe against a genuinely concurrent, uncommitted, legitimate rebuild in another process via a real two-connection WAL-isolation test, not a mock. Added a recovery runbook (`references/control-plane-rebuild-recovery.md`) for manually recovering a DB already in this corrupted state, codifying the lessons from this session's own live recovery incidents. Filed a comment on closed issue #523 with the live drift-risk evidence (its trigger-enforcement design can silently discard legitimate writes when a caller doesn't replicate the exact drop/recreate choreography) — not reopening #523, just its residual-risk record. | Fix Forward | RESOLVED — 9/9 new tests in `test_control_plane_schema_rebuild_atomicity.py` passing, including a RED/GREEN test reproducing the original atomicity failure against non-empty realistic data (the previously-missing test category identified during intake) and the two-connection WAL-concurrency test. Full `plugins/agent-agentic-os/tests/` suite: 234 passed, same 9 pre-existing unrelated failures confirmed byte-identical against unmodified `main` (`test_control_plane_policy.py`/`test_agent_control.py`/`test_control_plane_facade_composition.py` — a `policy.evaluate_transition`/`_run_rule` naming drift and an abstract-method gap, neither touched by this fix). Structural audit clean. Reviewed via two single-agent `cli-agents:security-auditor` dispatches — spec-stage (initial REVISE, 6 findings, all folded into the spec before implementation began) and implementation-stage (PASS, 1 MODERATE finding applied as a follow-up fix, 2 MINOR docstring nits applied) — matching #523's own two-round review precedent. |
| 2026-09-07 | Tier 2 | `coordinator.py`'s non-interactive `provided_answers` branch hardcoded `decision_actor = "human"` (comment claimed "non-interactive provided_answers represent human-supplied decisions delivered programmatically"), letting any programmatically-supplied answer satisfy the `enforce_valid_transition` DB trigger's `actor='human'` gate exactly like a genuine interactive stdin-pause answer — an agent could self-satisfy any `human_decision`-authority edge without a real human ever answering. Fixing the mislabel alone then unmasked three further latent test bugs that had never actually been exercised, each hidden behind an earlier assertion failure in the same test: (1) `test_worktree_push_barrier_enforcement` asserted a stale error-message regex, and once fixed, its second half wrongly expected `WORKTREE_REVIEW` (not `DONE`) to clear the push barrier — contradicted by the deliberate "enforce strict DONE state gate for push-to-origin" hardening already in `policy.py`; (2)/(3) `test_coordinator_artifact_resolution_inside_registered_worktree`'s second (`task_id_missing`) task-setup branch was missing two `stage_human_decisions()` calls (`AWAITING_APPROVAL->APPROVED`, `IN_WORKTREE->WORKTREE_REVIEW`) that its sibling `task_id` branch had; `test_trivial_fast_track_intake_to_done_records_decision_and_completes` used `provided_answers` against `intake_to_done_trivial`, an `authority.type: human_decision` edge, which after the actor fix now correctly requires a real interactive answer. | Changed `decision_actor` to `"agent"` in the non-interactive branch (`coordinator.py`), corrected the misleading inline comment. Fixed the three tests it unmasked: updated the stale regex; wrapped the `WORKTREE_REVIEW` push attempt in `pytest.raises` and added a real DONE-path assertion; switched the two human-gated `coordinate_transition()` calls (`test_coordinator_artifact_resolution_inside_registered_worktree`'s first branch, `test_trivial_fast_track_intake_to_done_records_decision_and_completes`) to `interactive=True` with a mocked `input_fn` instead of `provided_answers`, per this session's standing rule not to paper over actor-fix fallout by relabeling actor again; added the two missing `stage_human_decisions()` calls to the second branch. | Fix Forward | RESOLVED — `test_agent_control.py` 84/84, `test_human_decision_trigger_enforcement.py` 11/11, `-k control_plane` 79/79, all confirmed green after every fix, each new/changed test also re-run in isolation before the full-suite re-run. Diagram audit: `control-plane-architecture.mermaid` and both `control-plane-pipeline*.mermaid` files unaffected (no states/edges changed by this fix); noted a pre-existing, unrelated drift — the registry's `intake_to_done_trivial` (`INTAKE -> DONE`) edge is not drawn in `control-plane-pipeline.mermaid` — left as a separate follow-up, not touched here. |
trivial test line
| 2026-09-07 | Tier 1 | control-plane-reset-transition: no recovery path existed for a task whose control-plane record drifted from reality (e.g. a raw SQL write bypassing the normal transition path) other than `--no-verify` past the commit/push hooks — motivated by task-issue-pipeline-commit-hook's own record showing an unexplained `transition_violations` entry (`attempted_from_state=DONE`) inconsistent with its legitimate transition history. Along the way, fixing a `git diff` full-suite run also surfaced 9 pre-existing, unrelated test failures on this branch (an `evaluate_transition`/`_run_rule` naming drift in `policy.py`, an abstract `PersistencePort` fixture gap, and a stale two-option review string) that had nothing to do with this feature but were blocking a clean suite. | Added `reset_to_intake`: one authored `from_state: "*"` template in `transition_templates.yaml`, mechanically expanded by `TransitionRegistry.load_from_file()` into one literal edge per non-INTAKE canonical state (including DONE, an explicit deliberate exception to the "DONE is terminal" invariant, decided live with the user) — skipping any state with a pre-existing non-wildcard edge to INTAKE (e.g. ESCALATED) to avoid a duplicate-edge collision. Fully gated through the existing `coordinate_transition()`/DB-trigger path: `interactive=True` only (never satisfiable via `provided_answers`), mandatory non-empty justification, recorded as a new `decision_type='RESET'` (distinct from `ANSWER`/`SKIP`/`APPROVAL`/`CONFIRMATION`) so it's never mistaken for ordinary forward progress. `ALLOWED_TRANSITIONS` (state_machine.py) and the registry-driven `valid_transitions`/`required_transition_questions` DB tables both updated in lockstep (two independent authorities gate every edge in this system — missed either one and `validate_adjacency()` or the DB trigger would reject the transition). Also fixed forward, individually verified in isolation: added `policy.evaluate_transition()`/`_run_rule()` — deliberately as a local mapping inside the function, not a module-level table, per a pre-existing `test_policy_py_contains_no_transition_rules_or_to_state_rules` architecture test forbidding exactly that (Architecture Review Finding 6) — routing through the same `CHECK_REGISTRY` functions (`_prior_art_check`/`_done_check`/`_rolled_back_check`) already used elsewhere; added 5 missing abstract-method stubs to `test_control_plane_facade_composition.py`'s `_FakePersistencePort` fixture; fixed the stale `"Proceed with review [Recommended]"` string in `test_agent_control.py` (same root cause as the actor-fix PR #562 elsewhere tonight, just not yet rebased into this branch). Also replaced two hardcoded `== 52`/`== 64` edge-count assertions (and one stale docstring literal) with counts derived from `ALLOWED_TRANSITIONS`/`TransitionRegistry` at runtime, per explicit instruction that DAG-size assertions must never be hardcoded literals. | Fix Forward | RESOLVED — new `test_control_plane_reset_transition.py` (18 tests, fully data-driven over every applicable state via `RESET_EDGE_STATES` derived from the registry, not a hardcoded state list) plus the full `plugins/agent-agentic-os/tests/` suite: 261 passed, 1 skipped, 0 failed — every fix (the 9 pre-existing failures plus the 2 new hardcoded-count fixes) individually re-verified in isolation before the full-suite confirmation. Live-walked the actual recovery scenario end-to-end through the real CLI in `--interactive` mode with genuine human answers at every gate (review-entry skip, code-review skip) rather than `provided_answers`, matching this session's own guardrail against agent self-satisfaction of human-gated edges. |
| 2026-09-07 | Tier 0 | issue-551's progressive-disclosure SKILL.md trims created `acceptance-criteria.md`/`detailed-reference.md`/`fallback-tree.md` files directly inside individual `skills/<name>/references/` directories, violating the plugin-root hub-and-spoke pattern (ADR-002/003) — `audit_plugin_structure.py` flagged 43 violations, invisible to `audit.py` alone (per the documented gap: a structural violation only the second audit catches). | Moved each of the 43 files to a unique, skill-prefixed filename in the plugin root (e.g. `references/os-guide-fallback-tree.md`) — verified via diff first that each skill's content is genuinely unique, not boilerplate, so the audit tool's own literal suggested fix (identical target filename for 20+ different skills) would have silently overwritten all but the last one processed. Symlinked back via `symlink_manager.py create` for all 43, not raw `mv`/`ln -s`. | Fix Forward | RESOLVED — `audit_plugin_structure.py`: 0 violations (was 43); `audit.py` still passes; `symlink_manager.py diagnose`: all 43 links OK. |
| 2026-09-07 | Tier 0 | `coordinate_transition()`'s output gave no indication of what the legal next edges were after a transition — this session repeatedly re-derived "what's the actual next edge" by hand-reading `transition_templates.yaml`, causing several mid-session mistakes (stale question options quoted from memory, wrong edge assumed). | Added `_write_next_steps_hint()`: prints the legal next edges from the new state, live from `TransitionRegistry` (not memorized), flagged per-edge as requiring a human question or purely deterministic, plus a pointer to `interview-spec/SKILL.md` and the three control-plane diagrams. Added an optional `next_steps_hint` field to `TransitionTemplate`/the YAML schema for edge-specific guidance beyond `purpose`; falls back to `purpose` when unset — fully backward compatible, no template populates it yet. | Fix Forward | RESOLVED — full suite 261 passed, 1 skipped (unchanged from baseline); live-verified actual CLI output shows the new block correctly, including the `reset_to_intake` edge flagged as requiring a human question. |
| 2026-09-07 | Tier 2 | `ControlPlane.coordinate_transition()` (the thin `agent_control.py` facade) was missing `skip_review`/`skip_reason` params that `TransitionCoordinator.coordinate_transition()` already required, AND neither the `transition` nor `coordinate-transition` argparse subparsers registered `--skip-review`/`--skip-reason` at all — two independent signature-drift gaps in the same feature, both masked by `_dispatch_command()`'s silent `getattr(args, "skip_review", False)` fallback instead of a loud `AttributeError`. Discovered live resuming this very `link-cp-diagrams` task: `transition --to AWAITING_APPROVAL ... --skip-review` raised `TypeError`, and after fixing the facade signature alone, the real CLI command still failed with argparse's own `unrecognized arguments` since the flags weren't declared. | (1) Added `skip_review: bool = False, skip_reason: Optional[str] = None` to `ControlPlane.coordinate_transition()`, forwarded to `TransitionCoordinator.coordinate_transition()`. (2) Added `--skip-review`/`--skip-reason` to both `transition` and `coordinate-transition` subparsers in `_build_parser()`. | Fix Forward | RESOLVED — 2 new regression tests (`test_facade_coordinate_transition_forwards_skip_review_and_skip_reason`, `test_cli_transition_parsers_accept_skip_review_flags`), each RED (TypeError / unrecognized-arguments) before its fix, GREEN after. Full `plugins/agent-agentic-os/tests/` suite: 284 passed, 1 pre-existing skip. Live-verified via the actual CLI command that originally failed — `link-cp-diagrams` advanced to `AWAITING_APPROVAL` (transition_id=86) with the real `--skip-review --skip-reason` flags. |
| 2026-09-07 | Tier 2 | ISSUE-547 friction: the guarded plan writer was released only after the transition that required its artifacts, and the first local reinstall was blocked by sandbox permissions on `.agents/`. | Recorded the missing Socratic receipt, completed the supported transition, rewrote the staged plan with the guarded writer, escalated the local installer with approval, and verified the installed wrapper after adding its manifest link. | Fix Forward | RESOLVED for this task; plan-writer capability ordering remains a follow-up design issue. |
| 2026-09-07 | Tier 2 | Layer 3 trace append concurrency: two `record_trace.py append` calls issued in parallel both received sequence 2 and the same predecessor hash. | Preserved the append-only manifest, logged the defect in map debt, and will serialize all remaining trace writes for ISSUE-547. | Fix Forward | OPEN follow-up: add locking/atomic sequence allocation and a concurrent-writer regression test. |
| 2026-09-08 | Tier 1 | Native-first worktree routing had only coarse Claude/Antigravity intake detection; it could not report runtime capabilities or prove Codex native worktree support before selecting a path. | Added session-aware capability probing, explicit Codex capability evidence, a native-first worktree planner with `.worktrees/` placement validation and portable cleanup guidance, and documented that native facilities never bypass control-plane gates. | Fix Forward | Task 3 implementation complete; RED/GREEN capability and worktree routing tests passing. |
| 2026-09-08 | Tier 1 | Delegated agents had no persisted contract for objective, scope, authority, budget, fallback, or verifier evidence; execution could not be distinguished from an approved boundary. | Added idempotent SQLite delegation contract, execution, and verifier receipt tables; policy validation for approval, availability, budget, scope, and fallback substitution; and human renewal approval for changed boundaries. | Fix Forward | Task 4 implementation complete pending full-suite verification. |
| 2026-09-09 | Tier 1 | Task 1 of the transition-guidance work exposed that non-interactive `--answers --actor human` reached the SQLite trigger and surfaced a persistence failure instead of an explicit provenance error, obscuring the supported interactive-only human boundary. | Added RED regression coverage at the coordinator facade and trigger-enforcement boundary, then rejected programmatic human relabeling before persistence while preserving the real interactive path. | Fix Forward | GREEN — focused provenance and affected control-plane suites pass (104 tests). |
| 2026-09-09 | Tier 1 | Transition guidance explained lifecycle edges but left agents to infer how to execute work packages, tasks, slices, and individual execution steps, including artifacts, validation, handoff, and recovery evidence. | Added one advisory execution-unit contract to the registry/YAML snapshot and rendered it in transition reports without changing legal-transition or SQLite authority. | Fix Forward | GREEN — execution-guidance and transition-guidance contract tests pass; runtime reinstall completed in the isolated worktree. |
| 2026-09-09 | Tier 1 | The control-plane architecture was spread across Python, YAML, SQLite adapters, Mermaid diagrams, and skill guidance without one checked-in operational map, making state/edge drift hard to detect. | Added a durable architecture reference and contract test that compares its state and edge inventory with the canonical registry while preserving the Mermaid/SQLite authority boundaries. | Fix Forward | GREEN — overview plus architecture and pipeline diagram contracts pass (17 tests). |
| 2026-09-09 | Tier 1 | The pipeline had no repeatable production-boundary exercise for interview routing, reset recovery, illegal-edge rejection, or live registry edge coverage. | Added a temporary-SQLite simulator that delegates to ControlPlane/TransitionCoordinator and reports registry-derived edge scenarios without Git, subprocess, worktree, or repository-database side effects. | Fix Forward | GREEN — simulator and focused control-plane contracts pass (41 tests). |
| 2026-09-09 | Tier 1 | A first-time agent could receive a correct denial but still lack a repeatable protocol for checking authority, adaptive questions, state preservation, and persistence side effects. | Added a first-time-agent simulation protocol, one-round simulator API, explicit YAML recording guidance, and denial evidence for transitions, decisions, and receipts. | Fix Forward | GREEN — two formal rounds completed; protocol/simulator/guidance/overview contracts pass (21 tests). |
| 2026-09-09 | Tier 1 | The standard happy-path simulation reached the final DONE gate with valid test and leak receipts but failed because the asymmetric-persistence destination was not documented and the simulator used an invalid temporary destination. | Named the accepted `wiki/decisions/<task-id>.md` or `references/map-debt.md` destinations in YAML, added five standard-path handoff hints, and made the complete standard replay reusable. | Fix Forward | GREEN — standard path reaches DONE in 6.67s; guidance and simulator contracts pass. |
| 2026-09-09 | Tier 1 | The new required top-level `execution_guidance` validation caused a legacy malformed-registry test fixture to fail at the earlier root-schema check instead of reaching the malformed-template assertion it was intended to exercise. | Updated only that fixture to reuse the production guidance contract while preserving the intentionally incomplete template fields; no enforcement was weakened. | Fix Forward | GREEN — full affected suite: 432 passed. |
| 2026-09-09 | Tier 1 | Task 7 review found the standard-path simulator directly called `ControlPlane.transition()` for three legal edges, bypassing coordinator-level YAML questions, policy checks, and receipt staging while still claiming production-boundary coverage. | Added a failing boundary regression, routed `MULTI_AGENT_REVIEW -> AWAITING_APPROVAL`, `APPROVED -> IN_WORKTREE`, and `VERIFY_EXIT -> RETROSPECTIVE` through `TransitionCoordinator`, and retained direct transition use only for the intentional illegal-edge rejection scenario. | Fix Forward | GREEN — simulator suite: 10 passed; full affected suite: 433 passed; independent reviewer full plugin suite: 342 passed. |
| 2026-09-09 | Tier 1 | The `IN_WORKTREE -> WORKTREE_REVIEW` human question accepted a negative answer and still committed the transition; the YAML exposed choices but did not declare which answers authorize advancement, and the coordinator treated every declared option as approval. | Added optional YAML `accepted_answers` validation against declared options, enforced it before staging decisions or committing, exposed the question ID in interactive prompts, and updated the shared characterization-test helper to choose the accepted option only for this gate. | Fix Forward | GREEN — negative/positive regression passes, simulator coverage passes, and full affected suite: 434 passed. |
| 2026-09-09 | Tier 1 | The `WORKTREE_REVIEW -> VERIFY_EXIT` guidance named `code_review_or_skip` but did not tell an agent whether another human question was required, how to record the authorized skip, or where the alternative code-review question lived. | Clarified the deterministic direct-skip path with the exact `--skip-review --skip-reason` command and explicitly separated it from the `MULTI_AGENT_CODE_REVIEW` human-question branch. | Fix Forward | GREEN — transition-guidance boundary contract passes. |
| 2026-09-10 | Tier 1 | `assert_interview_exit_ready()` existed for source-assisted interview candidates but was not wired into either authoritative lifecycle transition front door, allowing pending candidates to exit `INTERVIEW`. | Added whole-interview pending-candidate enforcement to direct and coordinated transition commit paths while preserving exact-round checks and human/source authorization rules; added denial-and-recovery regressions. | Fix Forward | GREEN — focused P01 and facade/source tests: 7 passed; full suite attempt stalled without failure output. |
| 2026-09-10 | Tier 1 | P01 review found force-close authorization was evaluated before edge legality, non-interactive callers could spoof a human actor, and the any-state-to-DONE contract had drifted from normal `RETROSPECTIVE -> DONE` behavior and its diagrams/docs. | Validated adjacency before authorization in both coordinator and facade commit paths; required interactive human provenance for force-close; preserved normal retrospective completion; synchronized YAML, SQLite registry-derived edges, architecture inventory, Mermaid diagrams, and guidance; added regression coverage. | Fix Forward | GREEN — focused and relevant control-plane suite: 151 passed. |
| 2026-09-11 | Tier 1 | The force-close override still selected the exact `RETROSPECTIVE -> DONE` template and SQLite required-question trigger, so an explicitly authorized close could be denied by ordinary retrospective gates; the simulator also lacked deterministic interactive input. | Resolve the force-close template explicitly, bypass only ordinary completion checks after validating human authorization, and make the simulator supply deterministic interactive `FORCE_CLOSE` input; preserve adjacency and normal retrospective completion. | Fix Forward | Verification — targeted retrospective force-close simulation succeeds (`RETROSPECTIVE -> DONE`) and affected state-machine/simulator tests pass. |
