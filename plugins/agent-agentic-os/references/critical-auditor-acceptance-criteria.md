# Acceptance Criteria: critical-auditor

## ✅ Scenarios that must trigger
- "Run an adversarial audit on the agent-orchestration/ plugin triggers."
- "Scan the mutation loop codebase for bypass paths."
- "Red-team the eval gate — find ways to force a KEEP without a real score improvement."

## ❌ Scenarios that must NOT trigger
- "Create a new sub-agent for code quality."
- "Review my code style and suggest improvements."
