flowchart LR
    subgraph Sources ["Install Sources"]
        UVX["uvx / bootstrap.py<BR>(GitHub — no local clone)"]
        LocalSrc["plugins/<BR>(local clone)"]
    end

    subgraph Bridge ["Installer Scripts"]
        PA["plugin_add.py<BR>(interactive TUI — primary)"]
        BI["plugin_installer.py<BR>(single plugin / CI)"]
    end

    subgraph Agents ["Target Environments"]
        Central[".agents/<BR>(canonical store)<BR>Antigravity · Gemini · Copilot"]
        Claude[".claude/ → symlinks<BR>(Claude Code only)"]
    end

    UVX -->|"runs"| PA
    LocalSrc -->|"runs"| PA
    LocalSrc -->|"runs"| BI

    PA -->|"skills + commands<BR>+ rules + hooks"| Central
    BI -->|"skills + commands<BR>+ rules + hooks"| Central
    Central -.->|"symlinked"| Claude

    classDef source fill:#eee,stroke:#333,stroke-width:1px,stroke-dasharray: 5 5
    classDef bridge fill:#bbf,stroke:#333,stroke-width:2px
    classDef agent fill:#bfb,stroke:#333

    class UVX,LocalSrc source
    class PA,BI bridge
    class Central,Claude agent
