{
  "type": "prompt",
  "name": "bundle",
  "description": "Interactively package project files into a Markdown (.md) or ZIP archive.",
  "prompt": "Use the `context-bundler` skill to fulfill this request. Follow the Phase 1 (Discovery) and Phase 2 (Confirm) protocol before generating the final output. \n\nUser input: {{user_input}}"
}