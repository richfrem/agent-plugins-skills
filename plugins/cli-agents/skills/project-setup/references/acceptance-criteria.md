# Acceptance Criteria: project-setup

- **Scaffold Action**: Interactively asks setup configuration questions and correctly writes `.claude/` configs or `.agents/` configs based on user selection.
- **Validation**: Generated `CLAUDE.md` is under 200 lines and `.claude/settings.json` parses as valid JSON.
- **Universal reach**: Scaffolds `.agents/config.json` with correct settings.
