# Evolution Log — CLI Agents

| Date | Tier | Friction / Failure | Patch | Edit Type | Outcome |
|------|------|-------------------|-------|-----------|---------|
| 2026-09-08 | Tier 1 | Runtime skill instructions did not expose the final required-source and canonical critic-verdict contract implemented by the shared router. | Documented `--require-input`, source envelopes, and `record-critic-review` canonical verdict behavior in the Copilot and Claude runtime skills. | Documentation contract | RESOLVED — skill contract tests pass and the local plugin reinstall completed. |
| 2026-09-08 | Tier 1 | Review dispatch tests covered missing input and source markers only partially; the CLI normalized `REQUEST_CHANGES` for persistence but reported the non-canonical verdict back to the caller. | Added regression coverage for missing and empty required sources, optional `/dev/null`, persona-free source envelopes, CLI/API canonical verdict recording, and PASS-only gate satisfaction. Normalized the CLI success message to report persisted `REVISE`. | Fix Forward | RESOLVED — affected CLI/control-plane suites green; no backend is invoked for missing or empty required review input. |
| 2026-09-08 | Tier 1 | Runtime model selection had no versioned capability contract or native-runtime metadata | Added schema-v2 runtime metadata, capability tiers, fail-closed validation, and shared tier selection for CLI and control-plane consumers | Contract + enforcement | Task 2 implementation in progress; focused tests cover valid/invalid catalogs and tier selection |
