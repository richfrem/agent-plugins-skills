"""
Purpose:
    CLI Agents Provider Adapters Module. Namespace package grouping the
    per-provider adapter implementations (claude, copilot, gemini) used
    by the local-llm-bridge scripts.

Key Input Dependencies:
    - None (namespace package only; see individual adapter modules)
"""
