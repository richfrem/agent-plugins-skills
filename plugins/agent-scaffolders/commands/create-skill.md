---
name: create-skill
description: Create a new stateless skill in an existing plugin. NOT for sub-agents (use `create-sub-agent`) and NOT for skills that need stateful counters or session memory (use `create-stateful-skill`).
argument-hint: "[skill-name] [--plugin plugin-path]"
allowed-tools: Bash, Read, Write
---

# /agent-scaffolders:create-skill

Please run the `create-skill` skill immediately.

$ARGUMENTS
