---
name: create-sub-agent
description: Scaffold a new autonomous sub-agent with its own prompt, system instructions, and tool permissions. NOT for stateless procedural skills (use `create-skill`) and NOT for creating GitHub Actions workflows (use `create-agentic-workflow`).
argument-hint: "[agent-name] [--plugin plugin-path]"
allowed-tools: Bash, Read, Write
---

# /agent-scaffolders:create-sub-agent

Please run the `create-sub-agent` skill immediately.

$ARGUMENTS
