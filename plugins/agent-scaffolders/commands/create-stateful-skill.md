---
name: create-stateful-skill
description: Scaffold an advanced stateful agent skill with session memory and state management. NOT for simple stateless skills (use `create-skill`) and NOT for GitHub agentic workflows (use `create-agentic-workflow`).
argument-hint: "[skill-name] [--plugin plugin-path]"
allowed-tools: Bash, Read, Write
---

# /agent-scaffolders:create-stateful-skill

Please run the `create-stateful-skill` skill immediately.

$ARGUMENTS
