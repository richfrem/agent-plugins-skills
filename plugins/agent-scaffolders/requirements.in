# agent-scaffolders requirements
# Run: pip-compile requirements.in
#
# Most scripts in this plugin use the standard library only.
# The azure scaffolder (scaffold_azure_agent.py) requires:

azure-ai-projects
azure-identity
pyyaml

# Security Overrides
# cryptography (transitive via azure-identity): CVE-2026-69247 — PKCS#7 EnvelopedData
# decryption exposes a Bleichenbacher oracle via distinguishable errors/timing. Fixed in 50.0.0.
cryptography>=50.0.0
