# Evolution Log — Dev Utils

| Date | Tier | Friction / Failure | Patch | Edit Type | Outcome |
|------|------|-------------------|-------|-----------|---------|
| 2026-09-06 | Tier 1/2 | (1) next_number.py's project-root discovery walked up for the nearest `.git` directory, which is a FILE (not a directory) inside a git worktree, so `.is_dir()` was always False there and the walk silently skipped past the worktree to an unrelated ancestor or a wrong fallback — same root cause class as the ControlPlane db_path bug fixed the same day. (2) gh_issue_search.py's `consolidate_and_search_dedup()` computed a title-overlap similarity score per candidate but never fed it back into `has_existing_root_cause` — only 5 hardcoded, unrelated phrases could ever trigger consolidation; a perfect 1.0-scoring candidate still fell through to "create_new_issue" (GitHub Issue #520, discovered live this session when the tool failed to flag its own just-filed issue #519 as a duplicate). | (1) Replaced the manual `.git`-is-a-directory walk-up with `git rev-parse --show-toplevel`, which correctly resolves inside a worktree too (same fix pattern as agent_control.py). (2) Added `DEDUP_SCORE_THRESHOLD = 0.6`; candidates scoring at or above it now set `has_existing_root_cause=True` and recommend `comment_and_append_evidence`, closing the gap. | Fix Forward | RESOLVED — 2 new tests (worktree-resolution subprocess test, high-scoring-candidate test), 1 existing test's fixture adjusted to stay clearly below threshold (was accidentally sitting exactly at the new cutoff). 159/159 tests passing across both plugins. |
