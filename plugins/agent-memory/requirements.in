# agent-memory Dependencies (Intent File)
# Workflow (run from project root):
#   1. Edit this file
#   2. python -m piptools compile plugins/agent-memory/requirements.in --output-file plugins/agent-memory/requirements.txt
#   3. python -m pip install -r plugins/agent-memory/requirements.txt

# Core
python-frontmatter
pydantic

# Core Chroma DB
chromadb

# LangChain Base
langchain

# LangChain Integration
langchain-chroma
langchain-huggingface
langchain-community
langchain-text-splitters

# Embeddings
sentence-transformers
einops

# Security Overrides
protobuf>=4.25.3
# langchain-core CVE-2026-34070: satisfied transitively via langchain>=1.3.9 (pulls langchain-core>=1.4.9)
requests>=2.32.2
Pygments>=2.17.2
aiohttp>=3.14.1
langsmith>=0.8.18
langchain>=1.3.9
pydantic-settings>=2.14.2
# chromadb: CVE-2026-45829 (Critical) — no confirmed patch as of 2026-07.
# Mitigation: do NOT expose chromadb server to public internet. Monitor upstream.
