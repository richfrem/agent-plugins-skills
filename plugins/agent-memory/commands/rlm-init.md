---
name: rlm-init
description: Initialize the RLM semantic cache for a project — creates profiles, scans directories, and runs the first distillation pass
argument-hint: "[profile name]"
allowed-tools: Bash, Read, Write
---

# /agent-memory:rlm-init Command

Please run the `rlm-init` skill immediately.
$ARGUMENTS
